{
  "phrases": {
    "对应示例": "Related example",
    "源文件": "Source",
    "全部 API 索引": "All APIs",
    "用途与示例": "Usage and examples",
    "完整声明": "Full declarations",
    "应用生命周期": "Application lifecycle",
    "组件关联类型": "Component types",
    "布局、样式与主题类型": "Layout, style, and theme types",
    "颜色与尺寸 Token": "Color and size tokens",
    "字体、图片与图标资源": "Fonts, images, and icon resources",
    "示例": "Example",
    "类型和值见本页同组常量块。": "See the corresponding constant block on this page for types and values。"
  },
  "lines": {
    "DXUI 专属 AI 编程技能，提供完整公开 API 参考、组件用法和可运行的 Go Example，帮助 AI 编写和修改 DXUI 应用。": "A dedicated AI coding skill for DXUI, with complete public API references, component guides, and runnable Go examples for building and modifying applications.",
    "仓库：[dxui/dxui-skill](https://github.com/dxui-org/dxui-skill)": "Repository: [dxui/dxui-skill](https://github.com/dxui-org/dxui-skill)",
    "## 包含什么": "## What is included",
    "- **编程指导**：状态与事件、布局、样式、后台任务和多窗口的使用规则。": "- **Coding guidance**: State, events, layout, styles, background tasks, and multiple windows.",
    "- **完整 API 参考**：DXUI 根包、官方图标及目录包，以及公开别名涉及的类型和方法。": "- **Complete API references**: The root package, official icons and catalog, and types and methods exposed through public aliases.",
    "- **组件与Example**：组件行为说明，以及窗口、表单、主题和多窗口等完整程序。": "- **Components and examples**: Behavior guides and complete window, form, theme, and multi-window programs.",
    "- **同步检查**：核对技能内的 API 参考与本地 DXUI 源码是否一致。": "- **Synchronization checks**: Compare the bundled API references with a local DXUI checkout.",
    "AI 从 `SKILL.md` 读取使用指导，再按任务查阅相关参考和Example。": "The assistant reads `SKILL.md`, then loads only the references and examples needed for the task.",
    "## 怎么使用": "## How to use it",
    "先获取仓库：": "Clone the repository:",
    "按照所用 AI 工具的技能安装方式，将整个 `dxui-skill` 目录加入技能目录。保留 `SKILL.md`、`references`、`assets` 和 `scripts`，以便工具读取参考和Example。": "Follow your AI tool's skill installation instructions to add the entire `dxui-skill` directory. Keep `SKILL.md`, `references`, `assets`, and `scripts` together so the tool can access the supporting files.",
    "安装后，在任务中明确使用这个技能，例如：": "After installation, name the skill in your request. For example:",
    "请使用 dxui-skill，为当前 Go 项目添加一个设置窗口。": "Use dxui-skill to add a settings window to this Go project.",
    "窗口包含名称输入框、主题选择和保存按钮。": "Include a name input, theme selector, and Save button.",
    "先检查 go.mod 中的 DXUI 版本，再根据对应 API 实现。": "Check the DXUI version in go.mod before choosing the APIs.",
    "如果工具不支持技能安装，但能读取本地文件，可以让它先读取仓库中的 `SKILL.md`，再根据其中的链接查阅所需内容。": "If your tool cannot install skills but can read local files, ask it to read the repository's `SKILL.md` and follow its reference links.",
    "## 与 llms.txt 配合使用": "## Using it with llms.txt",
    "[llms.txt](./llms-txt) 提供在线文档入口；dxui-skill 将编程指导、API 参考和Example放在本地，适合在开发项目时使用。": "[llms.txt](./llms-txt) provides an online documentation index. dxui-skill keeps coding guidance, API references, and examples locally for project development.",
    "技能中的 API 参考对应特定源码版本。已有项目应以 `go.mod` 中实际使用的版本为准，生成代码后在本地构建并检查交互。": "The bundled API references target a particular source version. Use the version in your project's `go.mod`, then build locally and check the generated UI.",
    "让 AI 助手先读取 DXUI 文档，再帮助你编写界面。把下面的地址提供给支持网页读取的 AI 工具即可：": "Give a web-enabled AI assistant the following address so it can consult DXUI documentation before writing your UI:",
    "## 它包含什么": "## What it contains",
    "`llms.txt` 是用 Markdown 编写的文档导航，包含 DXUI 简介，以及教程、组件和公开 API 的链接与简短说明。AI 可以从中找到相关页面，再按需读取具体参数和Example。": "`llms.txt` is a Markdown documentation index with a DXUI introduction and annotated links to tutorials, components, and public APIs. An assistant can follow the relevant links for parameters and examples.",
    "本站的文件是文档索引，不包含全部文档正文。格式参考 [llms.txt 规范](https://llmstxt.org/)。": "This file is an index, not the full documentation. Its format follows the [llms.txt specification](https://llmstxt.org/).",
    "把文档入口和你的需求一起发给 AI。例如：": "Provide the documentation entry point together with your request. For example:",
    "请先读取 https://dxui-org.github.io/llms.txt，": "First read https://dxui-org.github.io/llms.txt,",
    "再查看状态与事件、Input 和 Button 的文档。": "then consult the State and Events, Input, and Button pages.",
    "用 Go 和 DXUI 编写一个包含名称输入框和提交按钮的窗口。": "Use Go and DXUI to build a window with a name input and a Submit button.",
    "点击提交后，在窗口中显示输入的名称。": "After submission, display the entered name in the window.",
    "请提供可以直接运行的完整 main.go，并说明运行方法。": "Provide a complete, runnable main.go and explain how to run it.",
    "已有项目时，再提供 `go.mod` 中的 DXUI 版本和相关代码，方便 AI 核对 API。": "For an existing project, also provide its DXUI version from `go.mod` and relevant code so the assistant can check API compatibility.",
    "## 工具不能读取网页时": "## When the tool cannot read web pages",
    "打开 [llms.txt](/llms.txt)，将内容粘贴给 AI；然后按任务需要，补充对应文档页的正文或完整Example。只提供链接并不代表工具已经读取了链接中的内容。": "Open [llms.txt](/llms.txt) and paste its contents into the conversation. Add the relevant page text or complete examples as needed. Supplying a URL alone does not mean the tool has read it.",
    "不同工具读取外部文档的方式不同，提供入口后可要求它列出实际参考的页面。生成代码后，在本地运行并检查交互效果。": "Tools access external documentation differently. Ask the assistant to list the pages it actually consulted, then run the generated code locally and check its behavior.",
    "# 应用、事件与诊断 API": "# Application, events, and diagnostics API",
    "组件之外的运行入口都在 `github.com/dxui-org/dxui`。本页解释用途与使用条件，精确签名、全部字段和源码注释见[运行时声明](/api/runtime)。": "Application APIs are in `github.com/dxui-org/dxui`. This page explains their use and constraints; see [runtime declarations](/api/runtime) for exact signatures, fields, and source comments.",
    "| API | 参数与返回值 | 使用条件与Related example |": "| API | Parameters and results | Requirements and examples |",
    "| NewApp(AppOptions) *App | 复制配置中的字体/快捷键等描述，返回管理主窗口和子窗口的 App。 | 不创建窗口；[快速开始](/guide/getting-started)。 |": "| NewApp(AppOptions) *App | Copies configuration such as fonts and shortcuts; returns an App managing the main and child windows. | Does not create a window; see [getting started](/guide/getting-started). |",
    "| App.Run(func() View) error | 无参 root 返回有效 View；阻塞到关闭或失败。 | main 线程直接调用，一次性 App；nil root 拒绝。 |": "| App.Run(func() View) error | A parameterless root returns a valid View; blocks until shutdown or failure. | Call directly on the main thread; each App runs once. Nil roots are rejected. |",
    "| App.RunResponsive(func(LayoutContext) View) error | 上下文提供逻辑 Width/Height。 | 合并 resize/scale 后重建；[响应式完整程序](/guide/layout)。 |": "| App.RunResponsive(func(LayoutContext) View) error | Context supplies logical Width/Height. | Rebuilds after coalesced resize/scale events; [responsive example](/guide/layout). |",
    "| App.Update(func()) error | 排队修改主窗口状态，FIFO UI 执行，一批后重建主窗口。 | 只接受运行中的非 nil 提交；[后台任务](/guide/async)。 |": "| App.Update(func()) error | Queues main-window state changes in FIFO order on the UI thread; rebuilds after a batch. | Requires a running App and a non-nil closure; [background tasks](/guide/async). |",
    "| App.Invalidate() error | 仅请求主窗口重建。 | 可跨 goroutine；子窗口用 Window.Invalidate。[多窗口更新](/guide/multi-window)。 |": "| App.Invalidate() error | Requests a main-window rebuild. | Safe across goroutines; use Window.Invalidate for children. [Multi-window updates](/guide/multi-window). |",
    "| App.CreateWindow(WindowOptions, func() View) (*Window, error) | 创建独立顶层子窗口，首帧成功后显示。 | UI 回调或 App.Update 内调用；[多窗口教程](/guide/multi-window)。 |": "| App.CreateWindow(WindowOptions, func() View) (*Window, error) | Creates an independent top-level child window, shown after its first frame succeeds. | Call in a UI callback or App.Update; [multiple windows](/guide/multi-window). |",
    "| App.Close() | 请求关闭主窗口和全部子窗口，无返回值。 | 跨 goroutine 安全且幂等；Run 前为空操作。 |": "| App.Close() | Requests shutdown of the main window and all children; no result. | Concurrent-safe and idempotent; a no-op before Run. |",
    "| App.SetTheme(Theme) error | 校验、复制主题并传播到存活窗口。 | 运行中在 UI 回调或 Update 内调用；[主题Example](/guide/style)。 |": "| App.SetTheme(Theme) error | Validates, copies, and propagates the theme to live windows. | During execution, call in a UI callback or Update; [theme example](/guide/style). |",
    "| App.SetClipboardText(string) error | 写入 UTF-8 文本，无效 UTF-8 替换为 U+FFFD。 | UI 回调/Update 中执行；关闭、未运行或原生剪贴板失败返回错误。 |": "| App.SetClipboardText(string) error | Writes UTF-8 text; invalid UTF-8 becomes U+FFFD. | UI callback/Update only; reports unavailable lifecycle or native clipboard errors. |",
    "| App.Diagnostics() RuntimeDiagnostics | 主窗口的线程安全值快照；子窗口用 Window.Diagnostics。 | 可在运行中或结束后取样；统计数据含义见下文。 |": "| App.Diagnostics() RuntimeDiagnostics | Concurrent-safe main-window snapshot; use Window.Diagnostics for children. | Available during or after execution; see the metrics below. |",
    "| `Assign[T](*T) func(T)` | 创建纯赋值回调；nil 指针立即 panic。 | 用于 UI 回调，不能替代 Update。 |": "| `Assign[T](*T) func(T)` | Creates an assignment callback; a nil pointer panics immediately. | For UI callbacks; does not replace Update. |",
    "| `Some[T](T) Option[T]` | 显式提供一个值，包括零值。 | Option 零值为未设置，没有公开 get 方法。 |": "| `Some[T](T) Option[T]` | Explicitly supplies a value, including zero. | Zero Option means unset; there is no public getter. |",
    "| ErrAppNotRunning / ErrAppClosed | 可用 errors.Is 匹配的生命周期错误。 | [后台任务关闭处理](/guide/async)。 |": "| ErrAppNotRunning / ErrAppClosed | Lifecycle errors matched with errors.Is. | [Background task shutdown](/guide/async). |",
    "App 与 Window 都提供标题、尺寸、最大化、最小化和状态读取方法；全部参数、线程要求与完整程序见[窗口管理 API](/api/windows)。": "App and Window both expose title, size, maximize/minimize, and state queries. See [window management](/api/windows) for all parameters, thread requirements, and a complete program.",
    "## AppOptions 字段": "## AppOptions fields",
    "| 字段 | 用途、默认与限制 |": "| Field | Purpose, defaults, and constraints |",
    "| Title | 窗口标题；空字符串默认 dxui。 |": "| Title | Window title; an empty string defaults to dxui. |",
    "| Width / Height | 初始逻辑尺寸，零值分别默认 800 / 600；有限非负且不超过 MaxInt32，显式正值四舍五入后须至少为 1。 |": "| Width / Height | Initial logical size; zero defaults to 800 / 600. Finite, nonnegative, at most MaxInt32; explicit positive values must round to at least 1. |",
    "| MinWidth / MinHeight | 窗口最小逻辑尺寸，0 不设置限制；非法尺寸由 Run 报错。 |": "| MinWidth / MinHeight | Minimum logical size; 0 leaves it unspecified. Run reports invalid dimensions. |",
    "| Renderer | RendererAuto 默认；RendererSoftware 请求命名的软件渲染器。 |": "| Renderer | Defaults to RendererAuto; RendererSoftware requests the named software renderer. |",
    "| Background | 原始 RGBAColor 窗口清屏颜色，用 RGBA 构造；全零默认 RGBA(28,30,36,255)。教程显式使用浅色背景。 |": "| Background | Raw RGBAColor clear color, constructed with RGBA. All-zero defaults to RGBA(28,30,36,255). Tutorials explicitly choose a light background. |",
    "| Caches | CacheBudgets，0 选默认，负数禁用相应缓存；预算按窗口分别应用。 |": "| Caches | CacheBudgets; 0 selects defaults, negative values disable the corresponding cache. Budgets apply separately to each window. |",
    "| Fonts | 从 FontBytes/FontFile/SystemFont 得到的 Font 列表；启动加载，错误由 Run 返回。 |": "| Fonts | Font descriptions from FontBytes/FontFile/SystemFont; loaded at startup, with errors returned by Run. |",
    "| DefaultFont | 默认 FontFamily；配合注册字体使用。 |": "| DefaultFont | Default FontFamily; use with registered fonts. |",
    "| DisableSystemFontFallback | 默认 false；true 关闭自动系统 CJK 回退，不改变显式注册字体。 |": "| DisableSystemFontFallback | Defaults to false; true disables automatic system CJK fallback without changing registered fonts. |",
    "| Theme | 零 Theme 选内置亮色；部分非零主题不自动补齐所有缺失映射。 |": "| Theme | Zero Theme selects the built-in light theme; a partially populated theme is not automatically completed. |",
    "| Shortcuts | []Shortcut，复制主窗口快捷键；子窗口通过 WindowOptions.Shortcuts 独立配置。 |": "| Shortcuts | Copied main-window shortcuts; children configure WindowOptions.Shortcuts separately. |",
    "| OnCloseRequest | 主窗口关闭请求；nil 默认关闭全部窗口，非 nil 时由回调决定是否调用 App.Close。 |": "| OnCloseRequest | Main-window close request; nil closes all windows. A callback decides whether to call App.Close. |",
    "| OnError | 在 UI 线程观察可恢复构建/回调失败；nil 时终止 Run 并返回错误。 |": "| OnError | Observes recoverable build/callback failures on the UI thread; nil terminates Run with the error. |",
    "| OnShown | 主窗口完整首帧呈现并显示成功后仅调用一次；子窗口使用 WindowOptions.OnShown。 |": "| OnShown | Called once after the main window's complete first frame is presented and shown; children use WindowOptions.OnShown. |",
    "| Diagnostics | 默认 false；true 开启有界事件/时间/资源统计。 |": "| Diagnostics | Defaults to false; true enables bounded event, timing, and resource metrics. |",
    "## 快捷键、剪贴板、关闭和诊断完整Example": "## Complete shortcuts, clipboard, shutdown, and diagnostics example",
    "Shortcut.Key 只接受当前公开 ShortcutKey：Enter、Backspace、0..9、Plus、Minus、Multiply、Divide、Decimal、Equals；不支持任意字符串键名。Modifiers 的 Shift/Control/Alt/Super/Primary 默认全 false，精确匹配。OnPress 是无参动作，Repeat 默认 false。编辑器和控件语义按键优先，因此例子使用 Primary+1 而非争用输入框 Enter。": "Shortcut.Key accepts only the public ShortcutKey values: Enter, Backspace, 0..9, Plus, Minus, Multiply, Divide, Decimal, and Equals. Arbitrary key strings are unsupported. Shift/Control/Alt/Super/Primary default to false and match exactly. OnPress takes no arguments; Repeat defaults to false. Editors and built-in control keys take priority, so the example uses Primary+1 rather than Enter.",
    "## CacheBudgets 与 RuntimeDiagnostics": "## CacheBudgets and RuntimeDiagnostics",
    "以下预算按窗口分别使用，当前各窗口 CPU 缓存也独立；应用总容量随存活窗口数增长。Window.Diagnostics 查看子窗口资源，App.Diagnostics 不是全窗口汇总。": "Budgets and CPU caches are independent per window, so total capacity grows with the number of live windows. Window.Diagnostics describes a child; App.Diagnostics is not an aggregate.",
    "| 缓存字段 | 零值默认预算 | 限制含义 |": "| Cache field | Default budget | Meaning |",
    "| FontBytes | 32 MiB | 应用字体和自动 CJK 字体源预算；负值不允许应用字体。 |": "| FontBytes | 32 MiB | Application and automatic CJK font sources; negative values disallow application fonts. |",
    "| TextSourceBytes | 2 MiB | 保留文本/图标掩码源；负值不允许这些保留源。 |": "| TextSourceBytes | 2 MiB | Retained text/icon mask sources; negative values disallow these retained sources. |",
    "| GlyphBytes | 2 MiB | 字形缓存。 |": "| GlyphBytes | 2 MiB | Glyph cache. |",
    "| TextMeasureBytes | 1 MiB | 文本测量缓存。 |": "| TextMeasureBytes | 1 MiB | Text measurement cache. |",
    "| ImageBytes | 2 MiB | 非活动、可复用的 CPU 图片/原生纹理；当前显示的唯一图片为工作集，不由此硬性封顶。 |": "| ImageBytes | 2 MiB | Inactive reusable CPU images/native textures; unique visible images form a working set not strictly capped by this budget. |",
    "| ShadowBytes | 2 MiB | 有界阴影几何缓存。 |": "| ShadowBytes | 2 MiB | Bounded shadow geometry cache. |",
    "RuntimeDiagnostics 的 RendererName/SoftwareFallback 用来确认渲染方式；LogicalSize/PixelSize/PixelDensity/DisplayScale 区分逻辑尺寸与像素缩放。FrameCount、BuildCount、LayoutCount、PaintCount、ReconcileCount 和 PaintNodeCount 区分呈现、构建、布局、绘制与树工作。": "RendererName/SoftwareFallback identify the rendering mode. LogicalSize/PixelSize/PixelDensity/DisplayScale distinguish logical dimensions and pixel scaling. FrameCount, BuildCount, LayoutCount, PaintCount, ReconcileCount, and PaintNodeCount distinguish presentation, building, layout, painting, and tree work.",
    "WindowCreates、RendererCreateAttempts/Creates、ExposeEvents、ResizeEvents、ScaleEvents、NoopViewportEvents、RendererResetEvents 用于生命周期与视口诊断；EventCount 为事件数。TextureCreates/Destroys、CacheBytes/BudgetBytes/Entries、FontResources/ImageResources/RendererResources 用于资源观察。Goroutines 和 GoHeapBytes/Objects、GoTotalAllocBytes、GoMallocs 是进程 Go 统计快照，不可解读为一个组件独占内存。": "WindowCreates, RendererCreateAttempts/Creates, ExposeEvents, ResizeEvents, ScaleEvents, NoopViewportEvents, and RendererResetEvents track lifecycle and viewport events; EventCount counts events. TextureCreates/Destroys, CacheBytes/BudgetBytes/Entries, and FontResources/ImageResources/RendererResources describe resources. Goroutines, GoHeapBytes/Objects, GoTotalAllocBytes, and GoMallocs are process-wide Go metrics, not memory owned by one component.",
    "EventToPresent 和 FrameTime 是 TimingSummary：Count 为总计数，Samples 为有界保留样本数，P50NS/P95NS/P99NS 单位为纳秒。先检查 CountersEnabled；未开启、无样本或平台未提供的数据不能解释为性能为零。字段类型逐项见[RuntimeDiagnostics](/api/runtime#api-runtimediagnostics)。": "EventToPresent and FrameTime use TimingSummary: Count is the total count, Samples is a bounded sample count, and P50NS/P95NS/P99NS are nanoseconds. Check CountersEnabled first; disabled or unavailable metrics and absent samples are not zero-cost measurements. See [RuntimeDiagnostics](/api/runtime#api-runtimediagnostics) for field types.",
    "# 组件共同契约": "# Common component contracts",
    "23 种组件的 Props 直接声明以下五个字段，不存在需要嵌套的公共 Props 包装器。": "All 23 components declare these five fields directly in their Props; there is no nested common-props wrapper.",
    "| 字段 | 用途、默认与限制 | Example |": "| Field | Purpose, defaults, and constraints | Example |",
    "| Key string | 默认空，以同父级的类型/位置匹配；非空时用稳定且同级唯一业务键。跨父级不保留身份。 | [状态与身份](/guide/state) |": "| Key string | Empty matches by type/position within one parent; nonempty keys must be stable and unique among siblings. Moving to another parent does not retain identity. | [State and identity](/guide/state) |",
    "| Style Style | 零值选默认；局部尺寸、间距和绘制属性。 | [样式](/guide/style) |": "| Style Style | Zero selects defaults; local dimensions, spacing, and painting. | [Styles](/guide/style) |",
    "| Token ComponentToken | 默认组件主题项；显式值选另一个已定义主题项。 | [主题](/api/theme) |": "| Token ComponentToken | Defaults to the component theme entry; an explicit value selects another defined entry. | [Themes](/guide/style) |",
    "| States StateStyles | 给真实状态提供绘制补丁；不产生状态，不修改布局。 | [状态覆盖](/guide/style#实际覆盖顺序) |": "| States StateStyles | Paint patches for actual interaction states; does not create states or change layout. | [Precedence](/guide/style#style-precedence) |",
    "| Pointer PointerBehavior | PointerAuto 默认；PointerNone 排除整个子树的指针参与。不是通用 Disabled。 | [布局命中](/guide/layout) |": "| Pointer PointerBehavior | Defaults to PointerAuto; PointerNone excludes the whole subtree from pointer participation. Not a generic Disabled flag. | [Layout and hit testing](/guide/layout) |",
    "所有构造函数返回不可变 View，不返回可变控件对象。`View.WithKey(string)` 与 `View.WithStyle(Style)` 返回独立描述；后者替换完整 Style。零 View 作为根或子项是错误，空 Box 则有效。": "Constructors return immutable View descriptions, not mutable widgets. `View.WithKey(string)` and `View.WithStyle(Style)` return independent descriptions; WithStyle replaces the entire Style. A zero View is invalid as a root or child; an empty Box is valid.",
    "事件在 UI 线程执行；nil 回调允许存在，其交互含义由组件决定，不能直接推导为 Disabled。悬停、焦点、按压、编辑组合、弹层活动和非受控滚动由运行时拥有。业务值如 Value/Checked/Open 由应用拥有。[具体矩阵与Example](/guide/state)。": "Events run on the UI thread. Nil callbacks have component-specific meanings and do not imply Disabled. The runtime owns hover, focus, press, IME composition, overlays, and uncontrolled scrolling. The application owns values such as Value/Checked/Open. See [state and examples](/guide/state).",
    "结构体、枚举和函数的Full declarations见[组件 API](/api/components)；共有 Style 和主题结构见[样式声明](/api/style)。": "See [component declarations](/api/components) for structs, enums, and functions, and [style declarations](/api/style) for shared Style and theme structures.",
    "以下声明由 Go AST 提取，保留源码英文契约和字段注释，隐藏私有字段；`struct {}` 表示外部不可直接配置的内部表示，不表示可以用零值替代构造函数。常量块保留 iota 上下文。": "These declarations are extracted from the Go AST, retaining English source contracts and field comments while hiding private fields. `struct {}` represents an opaque implementation, not permission to use a zero value instead of its constructor. Constant blocks retain iota context.",
    "# 更新日志": "# Changelog",
    "记录 DXUI 的功能、公开 API 与行为变化。": "DXUI features, public APIs, and behavior changes.",
    "## 2026-09-14 · 多窗口支持": "## 2026-09-14 · Multiple windows",
    "对应 DXUI 源码提交：`4e5ee7d`。": "DXUI source commit: `4e5ee7d`.",
    "- 新增 `App.CreateWindow`、`Window` 和 `WindowOptions`，支持创建独立子窗口。": "- Added `App.CreateWindow`, `Window`, and `WindowOptions` for independent child windows.",
    "- 新增主窗口与子窗口的标题、尺寸、最大化和最小化操作。": "- Added title, size, maximize, and minimize operations for main and child windows.",
    "- 新增 `App.Invalidate`、`Window.Update` 和 `Window.Invalidate`，支持按窗口更新界面。": "- Added `App.Invalidate`, `Window.Update`, and `Window.Invalidate` for updates targeting individual windows.",
    "- 新增 `Window.Close`、`Window.Closed` 和 `Window.Diagnostics`，用于关闭子窗口、查询关闭状态和读取诊断信息。": "- Added `Window.Close`, `Window.Closed`, and `Window.Diagnostics` for child shutdown, close status, and diagnostic snapshots.",
    "- 多个窗口共享事件循环，各自维护界面、焦点和输入状态；快捷键按窗口独立配置。": "- Windows share an event loop but maintain independent UI, focus, and input state. Shortcuts are configured per window.",
    "- `App.SetTheme` 更新所有存活窗口的主题；关闭主窗口会结束应用并关闭全部子窗口。": "- `App.SetTheme` updates all live windows; closing the main window ends the application and closes all children.",
    "# 官方图标与完整目录": "# Official icons and complete catalog",
    "`github.com/dxui-org/dxui/icon` 的每个构造器都无参数，返回不可变 `dxui.IconData`。用 `dxui.Icon(dxui.IconProps{Data: icon.Search()})` 显示；Size/Color/StrokeWidth 在视图上配置。[完整Example](/components/icon)。下表列出全部名称和别名；每行 Go 名称都可用作 `icon.名称()`。": "Each constructor in `github.com/dxui-org/dxui/icon` takes no arguments and returns immutable `dxui.IconData`. Display it with `dxui.Icon(dxui.IconProps{Data: icon.Search()})`; configure Size/Color/StrokeWidth on the view. See the [complete example](/components/icon). The table lists every name and alias; each Go name can be called as `icon.Name()`.",
    "只有确实需要图标搜索器时才导入 `github.com/dxui-org/dxui/icon/catalog`：`catalog.Icons` 使整个图标库可达，增大产物。普通界面按需调用 icon 构造器。": "Import `github.com/dxui-org/dxui/icon/catalog` only when building an icon search tool: `catalog.Icons` makes the entire library reachable and increases binary size. For ordinary interfaces, call individual icon constructors.",
    "## 版本与数量常量": "## Version and count constants",
    "`icon.Version` 是上游 Lucide 版本字符串；`icon.Count` 包含规范名称和别名；`icon.CanonicalCount` 仅统计规范名称。这些是编译期元数据，没有参数，也不触发图标加载。可用 `fmt.Println(icon.Version, icon.Count, icon.CanonicalCount)` 显示当前编译基线。": "`icon.Version` is the upstream Lucide version string. `icon.Count` includes canonical names and aliases; `icon.CanonicalCount` counts canonical names only. These compile-time constants take no arguments and do not load icons. Use `fmt.Println(icon.Version, icon.Count, icon.CanonicalCount)` to inspect the build baseline.",
    "## 全部构造器": "## All constructors",
    "    // Name 为 kebab-case，GoName 为精确 Go 构造器名。": "    // Name is kebab-case; GoName is the exact Go constructor name.",
    "| 名称 | Go 构造器（返回 dxui.IconData） |": "| Name | Go constructor (returns dxui.IconData) |",
    "# 公开 API 索引": "# Public API index",
    "以仓库快照为准；pre-v1 API 可能不兼容调整。每项链接到精确声明、源码契约与对应教程。包含根包、官方 icon、icon/catalog，以及 IconData 别名可调用的方法；不要求应用导入 internal。": "References target a source snapshot; pre-v1 APIs may change incompatibly. Each entry links to its exact declaration, source contract, and related tutorial. Includes the root package, official icon and icon/catalog packages, and methods accessible through IconData. Applications do not need internal imports.",
    "[更新日志](/api/coverage) · [图标全部名称](/api/icons)": "[Changelog](/api/coverage) · [All icon names](/api/icons)",
    "精确签名见[资源声明](/api/resources)；ImageSource/ImageFit 等也在[组件类型](/api/components)。资源描述应在 root builder 外创建复用。": "See [resource declarations](/api/resources) for exact signatures; ImageSource/ImageFit and related types are in [component types](/api/components). Create and reuse resource descriptions outside the root builder.",
    "## 字体": "## Fonts",
    "| API | 参数/结果 | 生命周期与限制 |": "| API | Parameters/results | Lifetime and constraints |",
    "| FontBytes(family, data) Font | FontFamily 名称和 TTF/OTF/TTC 字节 | 立即复制字节；只要 Font/App 可达即保有数据。 |": "| FontBytes(family, data) Font | FontFamily name and TTF/OTF/TTC bytes | Copies bytes immediately; data remains while the Font/App is reachable. |",
    "| FontFile(family, path) Font | 精确文件路径 | Run 启动时打开，文件不自动打包；保持到文本资源销毁，不热加载。 |": "| FontFile(family, path) Font | Exact file path | Opens at Run startup; files are not automatically bundled. Retained until text resources are destroyed; no hot reload. |",
    "| SystemFont(family) Font | 固定通用系统字体族 | 尝试当前 OS 固定路径表，找不到时报启动错误，不模糊搜索字体显示名。 |": "| SystemFont(family) Font | Fixed generic system family | Tries the current OS candidate paths; missing fonts cause startup errors. Does not search arbitrary display names. |",
    "| Font | Family、Weight、Slant、FaceIndex | 用构造函数提供隐藏源；FaceIndex 为 TTC 字面索引，普通字体用 0；注册到 AppOptions.Fonts。 |": "| Font | Family, Weight, Slant, FaceIndex | Use constructors for the hidden source; FaceIndex selects a TTC face, or 0 for a normal font. Register in AppOptions.Fonts. |",
    "| FontFamilyDefault | 内嵌 Go Regular | 覆盖基础拉丁字符，不包含 CJK。 |": "| FontFamilyDefault | Embedded Go Regular | Basic Latin coverage; no CJK. |",
    "| FontFamilySystemSans / Mono / CJK | system-ui / system-monospace / system-cjk | 固定平台候选，并非所有系统必有。 |": "| FontFamilySystemSans / Mono / CJK | system-ui / system-monospace / system-cjk | Fixed platform candidates; availability varies. |",
    "// 配置片段：将字体随应用提供，路径以运行目录为基础。": "// Configuration fragment: distribute the font with the app; paths use the working directory.",
    "font.FaceIndex = 2 // 仅在这份字体集合的该索引确为所需字面时使用": "font.FaceIndex = 2 // Use only if this collection has the intended face at index 2.",
    "回退考虑 TextStyle.Families、应用注册/default 字体、内嵌拉丁字体、懒加载系统 CJK 候选，最后替代符/缺字框。自动 CJK 回退默认启用，候选缺失不会让整个布局失败；显式 SystemFont 失败则是启动错误。要可重复发布，使用自己有权分发的字体文件或嵌入字节，并保留字体许可证；不能依赖开发机字体恰好存在。": "Fallback considers TextStyle.Families, registered/default application fonts, embedded Latin fonts, lazily loaded system CJK candidates, then replacement/missing-glyph boxes. Automatic CJK fallback is enabled by default; missing candidates do not fail the entire layout, but explicit SystemFont failures are startup errors. For reproducible distribution, bundle font files or bytes you may redistribute and retain their licenses; do not rely on fonts installed on a development machine.",
    "## 完整字体Example": "## Complete font example",
    "下面是无需外部字体文件的完整 FontBytes 程序，使用 dxui 当前依赖中的 Go Regular 字体字节。它演示字节注册和默认字体选择，仅显示拉丁字符；不把该字体当作 CJK 字体。": "This FontBytes program needs no external font file: it uses Go Regular bytes from DXUI's current dependencies. It demonstrates byte registration and default family selection for Latin text; this is not a CJK font.",
    "这份Example需保留 `golang.org/x/image` 模块依赖（`go mod tidy` 会按 dxui 的依赖图解析），所用字体为 Go 字体项目的 BSD 许可资源。": "The example needs `golang.org/x/image` (`go mod tidy` resolves it through DXUI's dependency graph). The Go font project distributes this font under a BSD license.",
    "## 图片": "## Images",
    "`ImageBytes([]byte)` 立即复制编码数据；`ImageFile(string)` 记录按需读取的路径；`ImageFromGo(image.Image)` 保留按契约不可修改的 Go 图像。ImageSource 是可比较句柄，不暴露像素读取方法。每次新构造可能产生新资源身份，应在 builder 外缓存并按业务需要替换。": "`ImageBytes([]byte)` copies encoded data immediately; `ImageFile(string)` records a path read on demand; `ImageFromGo(image.Image)` retains a Go image that must not be mutated. ImageSource is comparable and exposes no pixel reader. New constructors may create new resource identities; cache handles outside builders and replace them when needed.",
    "只解码 PNG、JPEG、GIF 首帧；不读取在线 URL，不动画，不异步解码。Image.MaxPixels 默认 16×1024×1024，限制解码尺寸；Avatar 共享资源管线，但不暴露 MaxPixels 字段。OnLoad(Size) 和 OnError(error) 是通知，nil 不阻止加载。[无外部素材的 Image 完整Example](/components/image)与[Avatar 完整Example](/components/avatar)可直接运行。": "Decodes PNG, JPEG, and only the first GIF frame. No online URLs, animation, or asynchronous decoding. Image.MaxPixels defaults to 16×1024×1024 and limits decoded size; Avatar uses the same pipeline but exposes no MaxPixels. OnLoad(Size)/OnError(error) are notifications; nil does not stop loading. Run the self-contained [Image](/components/image) and [Avatar](/components/avatar) examples.",
    "## 官方与自定义图标": "## Official and custom icons",
    "常见界面使用 `icon.Search()` 一类无参数构造器；全部 2,066 个名称/别名见[图标目录](/api/icons)。构造器返回 IconData，用 IconProps.Size/Color/StrokeWidth 配置绘制。普通应用无需导入 catalog；catalog.Icons 全量目录会让整个图标库进入可达集合。": "Use parameterless constructors such as `icon.Search()`; the [catalog](/api/icons) lists all 2,066 names and aliases. Constructors return IconData; IconProps.Size/Color/StrokeWidth configure rendering. Ordinary applications do not need catalog: catalog.Icons retains the entire library.",
    "自定义 IconData 用 ViewBox 和 []PathCommand 描述填充路径。PathMove/PathLine 使用 Points[0]，PathQuad 使用前两个点，PathCubic 使用三个点，PathClose 关闭路径。不是 SVG 语法，也没有运行时 SVG 解析。": "Custom IconData describes filled paths with ViewBox and []PathCommand. PathMove/PathLine use Points[0], PathQuad uses two points, PathCubic uses three, and PathClose closes a path. This is not SVG syntax and there is no runtime SVG parser.",
    "IconData 是公开别名，因此 `IsPacked() bool`、`Identity() uint64`、`Paths(maxCommands int)` 也可调用。IsPacked 区分官方不可变编码与自定义 Commands；Identity 是内容哈希，不保证无碰撞，不应用作业务主键；Paths 校验并解码，maxCommands 必须为正且足够，失败返回 error。": "IconData is a public alias, so `IsPacked() bool`, `Identity() uint64`, and `Paths(maxCommands int)` are callable. IsPacked distinguishes official immutable encodings from custom Commands. Identity is a content hash, not a collision-free business key. Paths validates and decodes; maxCommands must be positive and sufficient, otherwise it returns an error.",
    "Paths 的返回元素来自内部定义（含 Mode 与 Commands），外部可以通过类型推断访问，但不应导入 internal 来命名它。Mode 的内部值为描边 0、填充 1；这是别名带出的低层表示，组件应用应直接使用 IconData。不要修改官方图标内部描述或解码结果来期望改变资源。自定义 Commands 在构建 View 时复制。": "Paths returns internally defined elements with Mode and Commands, accessible through type inference without importing internal packages. Mode uses 0 for stroke and 1 for fill. These are low-level alias details; component code should use IconData directly. Do not mutate official descriptions or decoded results to change resources. Custom Commands are copied when building a View.",
    "# 主题与 Token API": "# Theme and token API",
    "`LightTheme() Theme`、`DarkTheme() Theme` 返回独立、可修改的完整主题。`App.SetTheme(Theme) error` 验证并复制主题，传播到所有存活窗口；运行中从 UI 回调或 Update 调用。新建子窗口采用 App 当前主题，Window 没有独立 SetTheme 方法。[完整明暗切换程序](/guide/style)。": "`LightTheme() Theme` and `DarkTheme() Theme` return independent, editable complete themes. `App.SetTheme(Theme) error` validates, copies, and propagates a theme to all live windows; call from a UI callback or Update while running. New children use the current App theme; Window has no SetTheme. [Complete theme switcher](/guide/style).",
    "| 类型/字段 | 用途 | 约束与Example |": "| Type/field | Purpose | Constraints and examples |",
    "| Theme.Primitive | PrimitiveTokens | Colors: map[ColorToken]RGBAColor；Metrics: map[MetricToken]float32。基础字面值。 |": "| Theme.Primitive | PrimitiveTokens | Colors: map[ColorToken]RGBAColor; Metrics: map[MetricToken]float32. Base literals. |",
    "| Theme.Semantic | SemanticTokens | Colors: map[ColorToken]ColorValue；Metrics: map[MetricToken]MetricValue。将用途映射为字面值或其它 Token。 |": "| Theme.Semantic | SemanticTokens | Colors: map[ColorToken]ColorValue; Metrics: map[MetricToken]MetricValue. Map semantic roles to literals or other tokens. |",
    "| Theme.Components | map[ComponentToken]ComponentTheme | 每个组件默认外观；非空 token 名。 |": "| Theme.Components | map[ComponentToken]ComponentTheme | Default component appearances; token names must be nonempty. |",
    "| ComponentTheme.Base | StylePatch | 组件基础绘制值。 |": "| ComponentTheme.Base | StylePatch | Base component paint values. |",
    "| ComponentTheme.States | StateStyles | 组件 Default/Hover/Focus/Checked/Pressed/Disabled 补丁。 |": "| ComponentTheme.States | StateStyles | Default/Hover/Focus/Checked/Pressed/Disabled patches. |",
    "| ColorToken / MetricToken / ComponentToken | string 定义的不同类型 | 用作对应 map 的键，不混用，不传未定义引用。 |": "| ColorToken / MetricToken / ComponentToken | Distinct string-based types | Keys for their respective maps; do not interchange them or reference undefined tokens. |",
    "| Color.Primitive.* | 可发现的调色板键 | 26 色族，每族 50、100..900、950，另有 White/Black；完整字段在 Token 声明页。 |": "| Color.Primitive.* | Discoverable palette keys | 26 families with 50, 100..900, and 950 shades, plus White/Black; see token declarations for all fields. |",
    "| Color.Semantic.* | Surface、SurfaceHigh、Text、Accent、AccentHover、OnAccent、FocusRing、Danger、Success、Info、Warn、Border、Shadow | 按语义定制，避免每个按钮硬编码调色板。 |": "| Color.Semantic.* | Surface, SurfaceHigh, Text, Accent, AccentHover, OnAccent, FocusRing, Danger, Success, Info, Warn, Border, Shadow | Customize by role rather than hardcoding a palette in each button. |",
    "// 片段：app 已创建；运行中在 UI 回调内执行。": "// Fragment: app already exists; execute in a UI callback while running.",
    "组件尺寸 Token 在 Semantic.Metrics 表中；先核对内置主题对应 map，再修改，避免同名键在错误层被遮盖。局部显式尺寸、padding、字体指标仍优先于主题默认。": "Component size tokens live in Semantic.Metrics. Check the built-in map before editing so a same-named key in another layer does not hide the change. Explicit local sizes, padding, and font metrics still override theme defaults.",
    "## Token 查找与使用": "## Finding and using tokens",
    "全部常量（包含旧平铺拼写）见[Token Full declarations](/api/tokens)。`ColorPrimitive*` 是原始颜色键，`ColorSemantic*` 是用途颜色键；`MetricPrimitive*` 是基础刻度，`MetricSemantic*` 是通用间距、圆角、字号等；`MetricComponent*` 是某组件的高度、间距、轨道、指示器尺寸；`Component*` 选择组件外观条目。": "See [all token declarations](/api/tokens), including legacy flat names. `ColorPrimitive*` names palette colors; `ColorSemantic*` names roles. `MetricPrimitive*` provides base scales; `MetricSemantic*` provides spacing, radii, font sizes, and similar values. `MetricComponent*` defines component dimensions; `Component*` selects appearance entries.",
    "比如 ProgressBar 的 `ColorSemanticProgressTrack/Fill` 分别控制轨道/填充；Tabs 的 `ColorSemanticTabsIndicator` 控制选中指示；Menu 的 Hover/Active/Selected/Pressed/Disabled 颜色分开。每个常量页保留真实字符串值，索引可搜索名字。它们是键名，不能直接当 RGBA 或 float32 使用，要包在 TokenColor/TokenMetric 中。": "For example, ProgressBar uses `ColorSemanticProgressTrack/Fill`; Tabs uses `ColorSemanticTabsIndicator`; Menu distinguishes Hover/Active/Selected/Pressed/Disabled colors. Reference pages retain actual string values, searchable by name. Tokens are keys, not RGBA or float32 values: wrap them in TokenColor/TokenMetric.",
    "Button 的 Variant/Tone 是常用外观入口，显式 Token 替换 Variant/Tone 配方，Size 仍独立生效。定制共享组件外观时用 `ComponentTheme`；仅一个按钮的例外用 Style/States。[按钮完整例子](/components/button)。": "Button Variant/Tone select common appearances. An explicit Token replaces that recipe while Size remains independent. Use `ComponentTheme` for shared changes and Style/States for one button. [Button example](/components/button).",
    "主题不接受缺失引用、循环依赖或非法数值。完全零 Theme 选内置亮色；创建一个部分非零 Theme 不等于“给默认主题打补丁”。SetTheme 相等或改变未使用值可不重绘，不能将其当作强制刷新接口。": "Themes reject missing references, cycles, and invalid numbers. A completely zero Theme selects the built-in light theme; a partially populated Theme is not a patch over the default. Equal themes or changes to unused values may not repaint; SetTheme is not a forced refresh API.",
    "# 几何、布局与绘制值": "# Geometry, layout, and paint values",
    "本页按用途解释共有 API；精确字段类型、枚举常量与源码契约见[样式Full declarations](/api/style)，坐标基础类型见[运行时声明](/api/runtime)。[布局完整Example](/guide/layout)和[样式完整Example](/guide/style)展示组合方式。": "This page groups shared APIs by purpose. See [style declarations](/api/style) for exact fields, enums, and contracts, and [runtime declarations](/api/runtime) for coordinates. The [layout](/guide/layout) and [style](/guide/style) examples demonstrate composition.",
    "## 坐标与构造函数": "## Coordinates and constructors",
    "| API | 参数与用途 | 零值、顺序与限制 |": "| API | Parameters and purpose | Zero values, order, and constraints |",
    "| Point{X,Y} / Size{Width,Height} / Rect{X,Y,Width,Height} | float32 逻辑位置、偏移、尺寸和矩形；图标中为 view-box 坐标。 | 不含后端对象。 |": "| Point{X,Y} / Size{Width,Height} / Rect{X,Y,Width,Height} | float32 logical positions, offsets, sizes, and rectangles; view-box coordinates for icons. | No backend objects. |",
    "| Px(float32) Length | 显式逻辑长度。 | 布局要求有限非负；零 Length 为 auto，不同于 Px(0)。 |": "| Px(float32) Length | Explicit logical length. | Layout requires finite nonnegative values; zero Length means auto, unlike Px(0). |",
    "| Percent(float32) / Fill() | 百分比 0..100；Fill 等价 Percent(100)。 | 父轴不确定时按 auto。 |": "| Percent(float32) / Fill() | Percentage 0..100; Fill equals Percent(100). | Acts as auto when the parent axis is indefinite. |",
    "| Metric(float32) MetricValue | 显式逻辑指标，可表达设置为 0。 | 与 Length 类型不同，用于 padding、边框等；约束依属性而定。 |": "| Metric(float32) MetricValue | Explicit logical metric, including 0. | Distinct from Length; used for padding, borders, etc. Constraints depend on the property. |",
    "| TokenMetric(MetricToken) | 读取主题指标。 | 缺失/循环引用被拒绝。 |": "| TokenMetric(MetricToken) | Reads a theme metric. | Missing or cyclic references are rejected. |",
    "| NoShrink() Option[float32] | Some(float32(0))。 | 与默认 shrink=1 不同。 |": "| NoShrink() Option[float32] | Some(float32(0)). | Unlike the default shrink factor of 1. |",
    "| Padding(n) / Margin(n) | 四边统一逻辑数值。 | 返回 EdgeValues，包含显式零。 |": "| Padding(n) / Margin(n) | Uniform logical spacing on four sides. | Returns EdgeValues with explicit zero support. |",
    "| PaddingXY(x,y) / MarginXY(x,y) | 水平与竖直边距。 | x 对应左右，y 对应上下。 |": "| PaddingXY(x,y) / MarginXY(x,y) | Horizontal and vertical spacing. | x is left/right; y is top/bottom. |",
    "| Edges(t,r,b,l) | 四边显式数值。 | 上、右、下、左；不支持负间距。 |": "| Edges(t,r,b,l) | Explicit values for four sides. | Top, right, bottom, left; negative spacing is unsupported. |",
    "| UniformEdges(MetricValue) | 四边同一字面/Token 指标。 | EdgeValues 各字段零值为未设置，可单独配置。 |": "| UniformEdges(MetricValue) | One literal/token metric for all sides. | Zero EdgeValues fields are unset; each can be configured separately. |",
    "| Round(n) / Corners(tl,tr,br,bl) | 统一/逐角逻辑圆角。 | 左上、右上、右下、左下；绘制时按几何钳制。 |": "| Round(n) / Corners(tl,tr,br,bl) | Uniform or per-corner logical radii. | Top-left, top-right, bottom-right, bottom-left; clamped to geometry when painted. |",
    "| UniformCorners(MetricValue) | 统一字面/Token 圆角。 | CornerValues 各字段零值为未设置。 |": "| UniformCorners(MetricValue) | Uniform literal/token radius. | Zero CornerValues fields are unset. |",
    "| RGBA(r,g,b,a) | uint8 非预乘 RGBAColor。 | 0..255，a=0 全透明。 |": "| RGBA(r,g,b,a) | Non-premultiplied uint8 RGBAColor. | 0..255; a=0 is transparent. |",
    "| ColorRGBA(r,g,b,a) / LiteralColor(RGBAColor) | 返回可设置样式的 ColorValue。 | 包括显式透明色；RGBAColor 与 ColorValue 不可混用。 |": "| ColorRGBA(r,g,b,a) / LiteralColor(RGBAColor) | Returns a ColorValue for styles. | Supports explicit transparency; RGBAColor and ColorValue are not interchangeable. |",
    "| TokenColor(ColorToken) | 返回 Token 引用 ColorValue。 | 当前主题负责解析。 |": "| TokenColor(ColorToken) | Returns a token-reference ColorValue. | Resolved by the current theme. |",
    "| Stroke(width,color) / NoBorder() | 实线 Border / 显式边宽 0。 | 普通 Style 后面的状态补丁仍可覆盖。 |": "| Stroke(width,color) / NoBorder() | Solid Border / explicit zero border width. | Later state patches can still override a normal Style. |",
    "## Style 每个字段": "## Every Style field",
    "| 字段 | 作用与默认 |": "| Field | Purpose and default |",
    "| Width/Height、MinWidth/MinHeight、MaxWidth/MaxHeight | Length 尺寸与限制，默认 auto；最小值优先于冲突最大值。 |": "| Width/Height, MinWidth/MinHeight, MaxWidth/MaxHeight | Length dimensions and limits, default auto; minima win over conflicting maxima. |",
    "| Margin/Padding | 外/内边距，EdgeValues 四边指标；未设置时组件可给默认。 |": "| Margin/Padding | Outer/inner spacing through EdgeValues; components may provide defaults when unset. |",
    "| Position | PositionFlow 默认；PositionAbsolute 退出 flex 流。 |": "| Position | Defaults to PositionFlow; PositionAbsolute leaves flex flow. |",
    "| Insets | Top/Right/Bottom/Left 的 Length 绝对位置约束。 |": "| Insets | Top/Right/Bottom/Left Length constraints for absolute positioning. |",
    "| Grow | 非负增长系数，默认 0。 |": "| Grow | Nonnegative growth factor, default 0. |",
    "| Shrink | Option[float32] 非负收缩系数，未设置为 1。 |": "| Shrink | Option[float32] nonnegative shrink factor, default 1 when unset. |",
    "| Basis | flex 基准 Length，默认 auto；收缩权重结合原始 basis。 |": "| Basis | Flex base Length, default auto; shrink weights include the original basis. |",
    "| AlignSelf | Option[Align]，未设置则使用父级 Align。 |": "| AlignSelf | Option[Align]; inherits parent Align when unset. |",
    "| ZIndex | 同级绘制顺序整数，默认 0；同值按源码顺序。 |": "| ZIndex | Sibling paint-order integer, default 0; ties use source order. |",
    "| Overflow | OverflowVisible 默认；OverflowClip 矩形裁剪。 |": "| Overflow | Defaults to OverflowVisible; OverflowClip clips to a rectangle. |",
    "| Background | ColorValue 背景；未设置沿用组件样式。 |": "| Background | ColorValue background; unset retains the component appearance. |",
    "| Border | 内侧绘制且不占布局空间；见下表。 |": "| Border | Painted inside without taking layout space; see below. |",
    "| Radius | CornerValues 四角；不等同圆角后代裁剪。 |": "| Radius | Four CornerValues; does not imply rounded descendant clipping. |",
    "| Shadow | 最多 4 个外阴影；nil 继承，非 nil 空切片清除。 |": "| Shadow | Up to 4 outer shadows; nil inherits, non-nil empty slice clears. |",
    "| Opacity | Option[float32]，0..1，乘到子树；0 不命中。 |": "| Opacity | Option[float32], 0..1, multiplied into the subtree; 0 excludes hit testing. |",
    "| Visibility | Visible 默认；Hidden 保留布局，不绘制/命中。 |": "| Visibility | Defaults to Visible; Hidden retains layout but does not paint or hit-test. |",
    "| Text | 文本样式与继承颜色，见下文。 |": "| Text | Text styling and inherited color; see below. |",
    "| Force | 最后应用的 StylePatch，仅绘制值，不能改变布局。 |": "| Force | Final StylePatch; painting only, cannot change layout. |",
    "Align 为 Start/Center/End/Stretch；Justify 为 Start/Center/End/SpaceBetween；Direction 为 Vertical/Horizontal。这些为枚举类型，使用公开常量，不自行构造越界整数。": "Align has Start/Center/End/Stretch; Justify has Start/Center/End/SpaceBetween; Direction has Vertical/Horizontal. Use the exported enum constants, not arbitrary integers.",
    "## Border、Shadow 与 TextStyle": "## Border, Shadow, and TextStyle",
    "Border.Width 为 MetricValue，Color 为 ColorValue，Pattern 为 BorderSolid（默认）或 BorderDashed，Sides 用 BorderTop/Right/Bottom/Left 位或组合；Sides=0 表示所有边，**不是无边框**。普通 Style 设置 Width/Color 时也会重置 Sides；StylePatch.Border 替换整个 Border。": "Border.Width is MetricValue; Color is ColorValue; Pattern is BorderSolid (default) or BorderDashed. Sides combines BorderTop/Right/Bottom/Left bits; Sides=0 means all sides, **not no border**. Setting Width/Color in a normal Style also resets Sides. StylePatch.Border replaces the entire Border.",
    "Shadow 的 OffsetX/OffsetY、Blur/Spread 都是 MetricValue，Color 为 ColorValue；偏移可带方向，Blur/Spread 必须有限非负。阴影是有界几何近似，非 CSS 高斯模糊；不影响布局和命中。": "Shadow OffsetX/OffsetY and Blur/Spread are MetricValue; Color is ColorValue. Offsets may be signed; Blur/Spread must be finite and nonnegative. Shadows use bounded geometry approximations, not CSS Gaussian blur, and do not affect layout or hit testing.",
    "TextStyle.Families 是有序 []FontFamily，Size/LineHeight 是逻辑字体大小/行高（0 取主题默认）；Weight 选最近注册字重，公开常量 WeightRegular=400、WeightMedium=500、WeightBold=700；SlantNormal/SlantItalic 选择字体斜体面，不承诺合成所有字体变体。Color 设置文字颜色，Align 为 TextStart/Center/End。只提供简单 LTR/CJK 文本，不支持 Arabic/Indic shaping、bidi/RTL、彩色 emoji、竖排。": "TextStyle.Families is an ordered []FontFamily. Size/LineHeight are logical metrics (0 uses theme defaults). Weight selects the nearest registered weight; constants include WeightRegular=400, WeightMedium=500, and WeightBold=700. SlantNormal/SlantItalic select font faces without promising synthesized variants. Color controls text; Align is TextStart/Center/End. Supports simple LTR/CJK text, not Arabic/Indic shaping, bidi/RTL, color emoji, or vertical text.",
    "## 覆盖补丁": "## Override patches",
    "StylePatch 的 Background、Border、Radius、Shadow、Opacity、Visibility、TextColor 全部是 Option，未设置沿用前一层，Some 显式覆盖（包括零值）。StateStyles 含 Default、Hover、Focus、Checked、Pressed、Disabled，每项都是 StylePatch；状态优先级见[样式教程](/guide/style)。": "StylePatch Background, Border, Radius, Shadow, Opacity, Visibility, and TextColor are Options. Unset inherits; Some explicitly overrides, including zero. StateStyles has Default, Hover, Focus, Checked, Pressed, and Disabled StylePatch fields. See [style precedence](/guide/style).",
    "# 窗口管理 API": "# Window management API",
    "一个 App 管理主窗口及零个或多个独立顶层子窗口。组件 API 仍统一来自 dxui 根包；Window 是生命周期句柄，不能作为 View 使用，也不要自行构造零值 Window。先读[多窗口教程](/guide/multi-window)，精确声明见[运行时参考](/api/runtime)。": "An App manages one main window and zero or more independent top-level child windows. Components remain in the root package; Window is a lifecycle handle, not a View, and should not be constructed as a zero value. Start with [multiple windows](/guide/multi-window); exact signatures are in [runtime references](/api/runtime).",
    "## 创建与调度": "## Creation and scheduling",
    "| API | 参数与结果 | 范围、限制与错误 |": "| API | Parameters and results | Scope, constraints, and errors |",
    "| App.CreateWindow(options WindowOptions, root func() View) (*Window, error) | 配置和非 nil 根构建函数；成功返回子窗口句柄。 | UI 回调或 App.Update 内调用。准备首帧后显示；失败清理已创建资源，不影响已有窗口。 |": "| App.CreateWindow(options WindowOptions, root func() View) (*Window, error) | Configuration and non-nil root builder; returns a child handle on success. | Call in a UI callback or App.Update. Shows after preparing the first frame; failures clean up partial resources without changing existing windows. |",
    "| App.Update(update func()) error | FIFO 排队执行非 nil 状态修改。 | 仅主窗口；可跨 goroutine。未运行/关闭返回 ErrAppNotRunning/ErrAppClosed。 |": "| App.Update(update func()) error | Queues non-nil state changes in FIFO order. | Main window only; safe across goroutines. Returns ErrAppNotRunning/ErrAppClosed outside its lifecycle. |",
    "| App.Invalidate() error | 请求主窗口重新构建，不附带状态修改。 | 可跨 goroutine；生命周期错误同 App.Update。不能替代后台状态同步。 |": "| App.Invalidate() error | Requests a main-window rebuild without changing state. | Concurrent-safe; lifecycle errors match App.Update. Does not synchronize background state writes. |",
    "| Window.Update(update func()) error | FIFO 排队执行非 nil 状态修改。 | 仅目标子窗口；可跨 goroutine。已关闭或 nil 句柄返回 ErrWindowClosed，App 正在退出可返回 ErrAppClosed。 |": "| Window.Update(update func()) error | Queues non-nil state changes in FIFO order. | Target child only; concurrent-safe. Closed/nil handles return ErrWindowClosed; shutdown may return ErrAppClosed. |",
    "| Window.Invalidate() error | 请求目标子窗口重建。 | 相当于对该窗口提交空更新；生命周期错误同 Window.Update。 |": "| Window.Invalidate() error | Requests a target child rebuild. | Equivalent to an empty update; lifecycle errors match Window.Update. |",
    "| Window.Close() | 请求关闭目标子窗口，无返回值。 | 幂等、可跨 goroutine；不关闭主窗口或兄弟窗口，不调用 OnCloseRequest。nil 句柄为空操作。 |": "| Window.Close() | Requests child shutdown; no result. | Idempotent and concurrent-safe. Leaves other windows open and bypasses OnCloseRequest. Nil is a no-op. |",
    "| Window.Closed() bool | 返回已请求或完成关闭。 | 并发安全，nil 句柄返回 true；不能作为下一次操作必定成功的保证。 |": "| Window.Closed() bool | Reports requested or completed closure. | Concurrent-safe; nil returns true. Does not guarantee a subsequent operation succeeds. |",
    "| Window.Diagnostics() RuntimeDiagnostics | 返回该窗口的计数快照。 | 并发安全；nil 句柄返回零快照。[字段解释](/api/application#cachebudgets-与-runtimediagnostics)。 |": "| Window.Diagnostics() RuntimeDiagnostics | Returns this window's metrics snapshot. | Concurrent-safe; nil returns a zero snapshot. [Fields](/api/application#cachebudgets-and-runtimediagnostics). |",
    "App.Close 关闭全部窗口并结束唯一事件循环；App.Diagnostics 仍以主窗口为对象，不是全部窗口统计之和。关闭后不要继续提交依赖该窗口存活的工作。": "App.Close closes every window and ends the shared event loop. App.Diagnostics still describes the main window rather than an aggregate. Do not submit work requiring a window after it closes.",
    "## WindowOptions 全部字段": "## All WindowOptions fields",
    "| 字段 | 类型 | 默认值与行为 |": "| Field | Type | Default and behavior |",
    "| Title | string | 空字符串默认 dxui。 |": "| Title | string | Empty defaults to dxui. |",
    "| Width / Height | float32 | 默认 640 / 480 逻辑单位；不同于主窗口默认的 800 / 600。有限非负，不超过 MaxInt32；显式正值四舍五入后至少为 1。 |": "| Width / Height | float32 | Defaults to 640 / 480 logical units, unlike the main window's 800 / 600. Finite, nonnegative, at most MaxInt32; explicit positive values must round to at least 1. |",
    "| MinWidth / MinHeight | float32 | 0 不设置最小尺寸；有限非负，不超过 MaxInt32。 |": "| MinWidth / MinHeight | float32 | 0 leaves minima unspecified; finite, nonnegative, at most MaxInt32. |",
    "| Background | RGBAColor | 全零使用 App 配置的背景；若仍全零，回退 RGBA(28,30,36,255)。 |": "| Background | RGBAColor | All-zero uses the App background, then RGBA(28,30,36,255) if still zero. |",
    "| Shortcuts | []Shortcut | 独立、复制的窗口快捷键；默认空，不自动继承 AppOptions.Shortcuts。 |": "| Shortcuts | []Shortcut | Independent copied shortcuts; empty by default, with no automatic inheritance from AppOptions.Shortcuts. |",
    "| OnCloseRequest | func(*Window) | nil 接受系统关闭请求；非 nil 时调用 Window.Close 才接受，否则拒绝。 |": "| OnCloseRequest | func(*Window) | Nil accepts system close requests. A callback must call Window.Close to accept; otherwise it rejects. |",
    "| OnShown | func(*Window) | 完整首帧呈现并显示后仅调用一次；在 CreateWindow 返回前执行，使用回调传入的句柄。 |": "| OnShown | func(*Window) | Called once after the complete first frame is presented and shown, before CreateWindow returns. Use its handle argument. |",
    "Theme、Fonts、DefaultFont、DisableSystemFontFallback、Renderer、Caches、Diagnostics 等使用 App 配置。窗口不暴露独立 SetTheme：通过 App.SetTheme 更新所有存活窗口。预算按窗口分别使用，创建更多窗口会增加总资源容量。": "Theme, Fonts, DefaultFont, DisableSystemFontFallback, Renderer, Caches, and Diagnostics use App configuration. Windows have no individual SetTheme; App.SetTheme updates all live windows. Budgets apply per window, so total resource capacity grows with more windows.",
    "源码 WindowOptions 注释目前笼统写了继承 App 快捷键，但创建实现明确用 options.Shortcuts 替换。因此本页与展示参考按实现说明为窗口独立配置，完整原始注释保留在维护快照中。": "The baseline WindowOptions source comment broadly claims shortcut inheritance, but creation explicitly replaces them with options.Shortcuts. This page and the displayed reference follow the implementation; the raw maintenance snapshot retains the original comment.",
    "## 主窗口与子窗口通用操作": "## Operations shared by main and child windows",
    "下面每个方法同时存在于 `*App`（主窗口）和 `*Window`（指定子窗口）。带原生操作的 SetTitle、SetSize、Maximize、Unmaximize、Minimize、Unminimize 必须在 UI 回调或 Update 闭包执行；Title、Size、IsMaximized、IsMinimized 是并发安全快照。": "Each method below exists on `*App` (main window) and `*Window` (target child). Native SetTitle, SetSize, Maximize, Unmaximize, Minimize, and Unminimize operations require a UI callback or Update closure. Title, Size, IsMaximized, and IsMinimized are concurrent-safe snapshots.",
    "| 方法 | 返回值 | 用途与限制 |": "| Method | Result | Purpose and constraints |",
    "| Title() | string | 最近成功配置的标题；App 未运行时返回 AppOptions 的原始标题。nil Window 返回空字符串。 |": "| Title() | string | Last successfully configured title; before Run, App returns the original option. Nil Window returns an empty string. |",
    "| SetTitle(title string) | error | 成功更新标题快照，不重建根；空字符串按请求设置，不套用创建时默认标题。 |": "| SetTitle(title string) | error | Updates the title snapshot on success without rebuilding the root. Empty strings are used literally, not replaced by the creation default. |",
    "| Size() | Size | 逻辑客户区尺寸；成功 SetSize 与后续 resize/scale 事件均更新快照。App 未运行时返回配置尺寸；nil Window 返回零 Size。 |": "| Size() | Size | Logical client size, updated by successful SetSize and later resize/scale events. Before Run, App returns configured dimensions; nil Window returns zero Size. |",
    "| SetSize(width, height float32) | error | 两轴必须有限正数，四舍五入后至少 1，不超过 MaxInt32。请求成功更新快照，实际布局跟随后续视口事件，平台仍可能调整最终尺寸。 |": "| SetSize(width, height float32) | error | Both dimensions must be finite, positive, round to at least 1, and not exceed MaxInt32. Success updates the snapshot; layout follows viewport events, and the platform may adjust the final size. |",
    "| Maximize() | error | 请求平台最大化；返回 nil 不代表状态事件已确认。 |": "| Maximize() | error | Requests maximization; nil does not mean a state event has confirmed it. |",
    "| Unmaximize() | error | 请求恢复正常状态。 |": "| Unmaximize() | error | Requests restoration to normal state. |",
    "| Minimize() | error | 请求平台最小化；返回 nil 不代表状态事件已确认。 |": "| Minimize() | error | Requests minimization; nil does not mean a state event has confirmed it. |",
    "| Unminimize() | error | 请求恢复正常状态；与 Unmaximize 共用平台恢复操作。 |": "| Unminimize() | error | Requests normal state; shares the platform restore operation with Unmaximize. |",
    "| IsMaximized() | bool | 最近窗口事件确认的最大化状态，不根据请求乐观设置；未创建主窗口或 nil Window 返回 false。 |": "| IsMaximized() | bool | State confirmed by the latest window event, not optimistically set by requests. False for an uncreated main window or nil Window. |",
    "| IsMinimized() | bool | 最近窗口事件确认的最小化状态；未创建主窗口或 nil Window 返回 false。 |": "| IsMinimized() | bool | Minimized state confirmed by the latest window event. False for an uncreated main window or nil Window. |",
    "主窗口命令在运行前返回 ErrWindowNotRunning，在 App 关闭后返回 ErrAppClosed。子窗口关闭后的命令返回 ErrWindowClosed；标题/尺寸/状态读取仍是快照，不用它们判断窗口是否存活。": "Main-window commands return ErrWindowNotRunning before Run and ErrAppClosed after closure. Closed-child commands return ErrWindowClosed. Title/size/state getters remain snapshots; do not use them as liveness checks.",
    "WindowOptions 不提供状态事件回调。若把 IsMaximized/IsMinimized 显示为文字，不应在发出请求后立即假定它已变化；下面的 Read confirmed state 按钮在点击时重新读取已确认状态。": "WindowOptions has no state-event callback. If displaying IsMaximized/IsMinimized, do not assume a request immediately changes them. The Read confirmed state button below reads confirmed state when clicked.",
    "## 完整操作Example": "## Complete window controls example",
    "这个例子给主窗口和子窗口提供同样的标题、尺寸、最大化与最小化按钮。子窗口首次构建早于 CreateWindow 返回，因此通过函数取得句柄，只在之后的按钮事件中执行操作。": "This example provides the same title, size, maximize, and minimize buttons for main and child windows. The child first builds before CreateWindow returns, so it retrieves the handle through a function only during later button events.",
    "最小化后可从系统任务栏或窗口管理器恢复窗口，或通过主窗口的 Restore child 按钮请求恢复。Refresh root 演示目标 Invalidate，Snapshot 演示目标 Diagnostics；这些接口不会创建轮询循环。": "After minimizing, restore through the taskbar/window manager or the main window's Restore child button. Refresh root demonstrates targeted Invalidate; Snapshot demonstrates Diagnostics. These APIs do not create polling loops.",
    "## 生命周期错误": "## Lifecycle errors",
    "| 错误 | 对应场景 |": "| Error | Scenario |",
    "| ErrWindowNotRunning | CreateWindow 在 App.Run 之外执行，或主窗口命令在运行前执行。 |": "| ErrWindowNotRunning | CreateWindow outside App.Run, or main-window commands before Run. |",
    "| ErrWindowClosed | nil/已关闭子窗口的 Update、Invalidate 或窗口命令。 |": "| ErrWindowClosed | Update, Invalidate, or commands on nil/closed children. |",
    "| ErrAppClosed | App 正在退出时提交工作或执行主窗口命令。 |": "| ErrAppClosed | Submitting work or executing main-window commands during shutdown. |",
    "| 其它 error | nil builder/更新闭包、非法尺寸、快捷键校验、首帧构建或平台操作失败。 |": "| Other errors | Nil builders/closures, invalid dimensions, shortcut validation, first-frame build failures, or platform failures. |",
    "使用 errors.Is 区分生命周期错误，处理返回值，不匹配错误文本。错误校验有先后顺序；例如 CreateWindow 在运行时已销毁后可能首先返回 ErrWindowNotRunning，不应把所有退出阶段一律看成同一种错误。": "Use errors.Is for lifecycle errors and handle return values instead of matching text. Checks have an order: CreateWindow may return ErrWindowNotRunning first after runtime destruction. Not every shutdown-stage failure has the same error.",
    "# Avatar：头像": "# Avatar: avatars",
    "用 `Avatar` 构建头像。本页包含可运行Example、完整属性及行为限制。": "Build avatars with `Avatar`. This page includes a runnable example, all props, and behavior constraints.",
    "## 完整Example": "## Complete example",
    "先完成[环境准备](/guide/getting-started)。下面是完整 `main.go`，执行 `go run .`。": "Complete [environment setup](/guide/getting-started) first. Copy this complete `main.go` and run `go run .`.",
    "## 参数与 API": "## Parameters and API",
    "| 字段 | 类型 | 用途、默认与约束 |": "| Field | Type | Purpose, defaults, and constraints |",
    "| `Key` | `string` | 同父级稳定唯一身份；默认空。动态列表使用业务 ID，勿用可变索引。 |": "| `Key` | `string` | Stable unique identity within one parent; empty by default. Use business IDs in dynamic lists, not changing indices. |",
    "| `Style` | `Style` | 局部布局、绘制和文本样式；零值使用固有尺寸与主题默认。 |": "| `Style` | `Style` | Local layout, paint, and text styling; zero uses intrinsic sizes and theme defaults. |",
    "| `Token` | `ComponentToken` | 组件主题条目；空值选择该组件默认。 |": "| `Token` | `ComponentToken` | Component theme entry; empty selects its default. |",
    "| `States` | `StateStyles` | 真实交互状态的绘制补丁，不能伪造状态或改变布局。 |": "| `States` | `StateStyles` | Paint patches for actual interaction states; cannot fabricate state or change layout. |",
    "| `Pointer` | `PointerBehavior` | 默认 PointerAuto；PointerNone 排除整个子树的指针参与，不等同 Disabled。 |": "| `Pointer` | `PointerBehavior` | Defaults to PointerAuto; PointerNone excludes the entire subtree from pointer participation, unlike Disabled. |",
    "| `Source` | `ImageSource` | 稳定 ImageSource；复用资源句柄，避免每次构建重新分配。 |": "| `Source` | `ImageSource` | Stable image handle; reuse it rather than allocating during every build. |",
    "| `Shape` | `AvatarShape` | 默认 AvatarCircle；AvatarSquare 为方形。 |": "| `Shape` | `AvatarShape` | Defaults to AvatarCircle; AvatarSquare selects a square. |",
    "| `Size` | `float32` | 尺寸；Button 是枚举，其它组件为逻辑单位，0 取主题默认。 |": "| `Size` | `float32` | Logical size; 0 uses the theme default. |",
    "| `OnLoad` | `func(Size)` | 加载成功通知，参数为解码 Size；nil 仍加载。 |": "| `OnLoad` | `func(Size)` | Success notification with decoded size; nil still loads. |",
    "| `OnError` | `func(error)` | 资源错误通知；应用可切换占位视图，nil 仍尝试加载。 |": "| `OnError` | `func(error)` | Resource error notification; the app may render a fallback. Nil still attempts loading. |",
    "## 行为与边界": "## Behavior and limitations",
    "共享 ImageSource 和 OnLoad/OnError，固定居中 Cover。Shape 默认 AvatarCircle，可选 AvatarSquare。Size=0 用主题默认 40，正数为逻辑尺寸；显式 Style 宽高优先。没有 URL、文字缩写、自动降级或状态徽点；需要降级时由应用根据 OnError 改渲染内容。": "Shares ImageSource and OnLoad/OnError, with centered Cover fitting. Shape defaults to AvatarCircle; AvatarSquare is optional. Size=0 uses the theme default of 40; positive values are logical units, overridden by explicit Style dimensions. No URLs, initials, automatic fallback, or status dots. Render fallback content in response to OnError.",
    "所有组件共享[身份、样式与指针规则](/api/common)。字段引用的枚举、结构体及源码注释见[Full declarations参考](/api/components)。[Example运行方式](/examples/)包含构建验证方法。": "All components share [identity, style, and pointer rules](/api/common). See [full declarations](/api/components) for referenced enums, structs, and comments. [Running examples](/examples/) explains build verification.",
    "# Badge：徽章标签": "# Badge: badges",
    "用 `Badge` 构建徽章标签。本页包含可运行Example、完整属性及行为限制。": "Build badges with `Badge`. This page includes a runnable example, all props, and behavior constraints.",
    "接受一个任意子 View，继承文字/图标色。默认紧凑胶囊形；无事件、焦点或保留交互状态。不内置通知角标定位、数字封顶、关闭、状态颜色变体或动画，应用用布局和 Style 组合。": "Accepts one arbitrary child View and inherits text/icon color. Defaults to a compact pill. No events, focus, or retained interaction state. Notification positioning, count caps, dismissal, status variants, and animation are not built in; compose layout and Style as needed.",
    "# Box：容器与布局": "# Box: containers and layout",
    "用 `Box` 构建容器与布局。本页包含可运行Example、完整属性及行为限制。": "Build containers and layouts with `Box`. This page includes a runnable example, all props, and behavior constraints.",
    "| `Direction` | `Direction` | 默认 Vertical；Horizontal 从左到右排列。 |": "| `Direction` | `Direction` | Defaults to Vertical; Horizontal arranges left to right. |",
    "| `Gap` | `float32` | 相邻子项的非负固定逻辑间距，默认 0。 |": "| `Gap` | `float32` | Nonnegative fixed logical spacing between children, default 0. |",
    "| `Justify` | `Justify` | 主轴 Start/Center/End/SpaceBetween，默认 Start。 |": "| `Justify` | `Justify` | Main-axis Start/Center/End/SpaceBetween, default Start. |",
    "| `Align` | `Align` | 交叉轴 Start/Center/End/Stretch，默认 Start。 |": "| `Align` | `Align` | Cross-axis Start/Center/End/Stretch, default Start. |",
    "默认 Vertical，单行排列；Gap 为逻辑单位。Justify 控制主轴，Align 控制交叉轴。children 可为空，但不能包含零 View。不支持换行、order、反向排列或完整 CSS Flexbox。": "Defaults to Vertical and a single row/column; Gap uses logical units. Justify controls the main axis, Align the cross axis. Children may be empty but cannot contain zero Views. No wrapping, order, reverse direction, or full CSS Flexbox.",
    "# ButtonGroup：连接按钮组": "# ButtonGroup: connected buttons",
    "用 `ButtonGroup` 构建连接按钮组。本页包含可运行Example、完整属性及行为限制。": "Build connected buttons with `ButtonGroup`. This page includes a runnable example, all props, and behavior constraints.",
    "| `Orientation` | `ButtonGroupOrientation` | 默认水平（ButtonGroup）或垂直（Menu）；详见本页。 |": "| `Orientation` | `ButtonGroupOrientation` | Defaults to horizontal; see behavior below. |",
    "| `Dividers` | `bool` | 默认 false；true 绘制组内主题分隔边。 |": "| `Dividers` | `bool` | Defaults to false; true draws themed separators. |",
    "仅接收 Button（含 TextButton），允许零个。默认水平且没有分隔线；Orientation=ButtonGroupVertical 改为纵向。Dividers 启用主题边框重叠，使共享边只绘制一次。组无独立焦点，每个按钮保留 Disabled、回调和源顺序 Tab；没有方向键导航。纵向自动宽度对齐最宽项，显式子 Width 优先。": "Accepts only Button (including TextButton), with zero children allowed. Horizontal without dividers by default; ButtonGroupVertical selects vertical. Dividers overlaps theme borders so each shared edge is painted once. No group focus; buttons retain Disabled, callbacks, and source-order Tab behavior. No arrow navigation. Vertical auto-width aligns to the widest button; explicit child Width wins.",
    "# Button：按钮与 TextButton": "# Button: buttons and TextButton",
    "用 `Button` 构建按钮与 TextButton。本页包含可运行Example、完整属性及行为限制。": "Build buttons with `Button`. This page includes a runnable example, all props, and behavior constraints.",
    "| `Variant` | `ButtonVariant` | 默认 ButtonFilled；外观配方，详见本页。 |": "| `Variant` | `ButtonVariant` | Defaults to ButtonFilled; see appearance recipes below. |",
    "| `Tone` | `ButtonTone` | 默认 ButtonPrimary；ButtonDefault 是其别名。 |": "| `Tone` | `ButtonTone` | Defaults to ButtonPrimary; ButtonDefault is an alias. |",
    "| `Size` | `ButtonSize` | 尺寸；Button 是枚举，其它组件为逻辑单位，0 取主题默认。 |": "| `Size` | `ButtonSize` | Size enum; defaults to ButtonNormal. |",
    "| `Disabled` | `bool` | 默认 false；交互组件为 true 时移除焦点并取消交互。 |": "| `Disabled` | `bool` | Defaults to false; true removes focus and cancels interaction on interactive components. |",
    "| `OnPress` | `func()` | 激活后在 UI 线程执行；nil 不执行动作。 |": "| `OnPress` | `func()` | Runs on the UI thread after activation; nil performs no action. |",
    "Button 接受一个任意子 View；TextButton 自动居中显示文字。默认 Filled / Primary / Normal。Variant 支持 Filled、Soft、Outline、Dashed、Ghost、Link；Tone 支持 Primary、Secondary、Success、Info、Warn、Danger；Size 支持 Small、Normal、Large。显式 Token 替换外观配方，局部 Style 保持覆盖权。指针或 Enter/Space 匹配释放时激活一次；nil OnPress 仍保留焦点与交互外观。Link 不执行导航。": "Button accepts one arbitrary child View; TextButton centers text. Defaults: Filled / Primary / Normal. Variants: Filled, Soft, Outline, Dashed, Ghost, Link. Tones: Primary, Secondary, Success, Info, Warn, Danger. Sizes: Small, Normal, Large. Explicit Token replaces the appearance recipe; local Style still overrides it. A matching pointer or Enter/Space release activates once. Nil OnPress retains focus and interaction appearance. Link does not navigate.",
    "# Checkbox：复选框": "# Checkbox: checkboxes",
    "用 `Checkbox` 构建复选框。本页包含可运行Example、完整属性及行为限制。": "Build checkboxes with `Checkbox`. This page includes a runnable example, all props, and behavior constraints.",
    "| `Checked` | `bool` | 当前受控布尔值，默认 false；在 OnChange 中接纳提案。 |": "| `Checked` | `bool` | Controlled boolean, default false; accept changes in OnChange. |",
    "| `OnChange` | `func(bool)` | 完整新值提案；写回业务变量后下一次构建生效。nil 行为见本页。 |": "| `OnChange` | `func(bool)` | Proposes a complete new value; write it to state for the next build. See behavior for nil callbacks. |",
    "接收一个标签 View。Checked 为受控布尔值，鼠标或 Space 提案取反；nil OnChange 不改变业务值，不等同 Disabled。Disabled 移除焦点并取消按压。无三态。": "Accepts one label View. Checked is controlled; pointer or Space proposes toggling it. Nil OnChange does not change the business value and is not Disabled. Disabled removes focus and cancels pressing. No tri-state support.",
    "# Icon：矢量图标": "# Icon: vector icons",
    "用 `Icon` 构建矢量图标。本页包含可运行Example、完整属性及行为限制。": "Build vector icons with `Icon`. This page includes a runnable example, all props, and behavior constraints.",
    "| `Data` | `IconData` | 有效 IconData；可用官方 icon 构造器或自定义填充路径。 |": "| `Data` | `IconData` | Valid icon data from an official constructor or custom filled paths. |",
    "| `Color` | `ColorValue` | 图标显式颜色或 TokenColor；未设置时继承主题色。 |": "| `Color` | `ColorValue` | Explicit color or TokenColor; inherits theme color when unset. |",
    "| `StrokeWidth` | `IconStrokeWidth` | 有限非负 view-box 描边宽，0 为 2；填充路径忽略。 |": "| `StrokeWidth` | `IconStrokeWidth` | Finite nonnegative view-box stroke width; 0 means 2. Ignored for filled paths. |",
    "Size 是逻辑单位，0 使用主题默认 20。StrokeWidth 为 view-box 单位，0 表示默认 2，必须有限且非负；对自定义填充图标忽略。图标无语义动作，需要组合 Button。icon.Name() 按需链接；icon/catalog 会引入完整目录。IconData 不是 SVG 解析器。": "Size uses logical units; 0 selects the theme default of 20. StrokeWidth uses view-box units, must be finite/nonnegative, and defaults to 2 when zero; custom filled icons ignore it. Icons have no action semantics; compose with Button. Individual icon.Name() calls link on demand, while icon/catalog retains the full catalog. IconData does not parse SVG.",
    "# Image：栅格图片": "# Image: raster images",
    "用 `Image` 构建栅格图片。本页包含可运行Example、完整属性及行为限制。": "Build raster images with `Image`. This page includes a runnable example, all props, and behavior constraints.",
    "| `Fit` | `ImageFit` | 默认 ImageContain；支持 Cover、Fill、None。 |": "| `Fit` | `ImageFit` | Defaults to ImageContain; also supports Cover, Fill, and None. |",
    "| `Alignment` | `Point` | X/Y 均为 0..1；默认 (0,0) 左上，(.5,.5) 居中。 |": "| `Alignment` | `Point` | X/Y each range from 0 to 1; default (0,0) is top-left, (.5,.5) is centered. |",
    "| `MaxPixels` | `int64` | 解码像素预算；0 选引擎默认 16,777,216，负值无效。 |": "| `MaxPixels` | `int64` | Decoded pixel budget; 0 selects 16,777,216, and negative values are invalid. |",
    "Source 由 ImageBytes、ImageFile 或 ImageFromGo 构造；放在 builder 外复用。Fit 默认 Contain（保比例完整显示），Cover 填满并裁剪，Fill 拉伸，None 原始尺寸。Alignment 每轴 0..1，默认左上。MaxPixels 限制解码像素数；只有 PNG/JPEG/单帧 GIF，无网络 URL 或异步解码。nil 加载回调不阻止解码。Go 图像构造后不得修改。": "Construct Source with ImageBytes, ImageFile, or ImageFromGo and reuse it outside builders. Contain preserves the entire image and aspect ratio; Cover fills and crops; Fill stretches; None keeps original size. Alignment is 0..1 per axis, default top-left. MaxPixels limits decoded pixels. Only PNG/JPEG/first-frame GIF; no network URLs or asynchronous decoding. Nil load callbacks do not prevent decoding. Do not modify a Go image after constructing its source.",
    "# 组件参考": "# Component reference",
    "每页包含可独立运行的完整程序、全部 Props 字段、交互行为和限制。`Label` 合并在 Text 页，`TextButton` 合并在 Button 页。": "Every page includes a standalone program, all Props fields, behavior, and limitations. `Label` is covered with Text; `TextButton` with Button.",
    "- [Box：容器与布局](/components/box)": "- [Box](/components/box)",
    "- [Text：文本与 Label](/components/text)": "- [Text](/components/text)",
    "- [Button：按钮与 TextButton](/components/button)": "- [Button](/components/button)",
    "- [ButtonGroup：连接按钮组](/components/button-group)": "- [ButtonGroup](/components/button-group)",
    "- [InputGroup：带前后缀的输入框](/components/input-group)": "- [InputGroup](/components/input-group)",
    "- [Input：单行输入](/components/input)": "- [Input](/components/input)",
    "- [Textarea：多行输入](/components/textarea)": "- [Textarea](/components/textarea)",
    "- [Checkbox：复选框](/components/checkbox)": "- [Checkbox](/components/checkbox)",
    "- [Radio：单选按钮](/components/radio)": "- [Radio](/components/radio)",
    "- [ToggleSwitch：开关](/components/toggle-switch)": "- [ToggleSwitch](/components/toggle-switch)",
    "- [Slider：滑块](/components/slider)": "- [Slider](/components/slider)",
    "- [Select：下拉选择](/components/select)": "- [Select](/components/select)",
    "- [Tabs：标签页选择器](/components/tabs)": "- [Tabs](/components/tabs)",
    "- [Menu：菜单动作列表](/components/menu)": "- [Menu](/components/menu)",
    "- [Scroll：滚动容器](/components/scroll)": "- [Scroll](/components/scroll)",
    "- [VirtualList：固定行高虚拟列表](/components/virtual-list)": "- [VirtualList](/components/virtual-list)",
    "- [Popover：交互弹层](/components/popover)": "- [Popover](/components/popover)",
    "- [Tooltip：提示浮层](/components/tooltip)": "- [Tooltip](/components/tooltip)",
    "- [Icon：矢量图标](/components/icon)": "- [Icon](/components/icon)",
    "- [Image：栅格图片](/components/image)": "- [Image](/components/image)",
    "- [Avatar：头像](/components/avatar)": "- [Avatar](/components/avatar)",
    "- [Badge：徽章标签](/components/badge)": "- [Badge](/components/badge)",
    "- [ProgressBar：进度条](/components/progress-bar)": "- [ProgressBar](/components/progress-bar)",
    "# InputGroup：带前后缀的输入框": "# InputGroup: input prefixes and suffixes",
    "用 `InputGroup` 构建带前后缀的输入框。本页包含可运行Example、完整属性及行为限制。": "Build input groups with `InputGroup`. This page includes a runnable example, all props, and behavior constraints.",
    "InputGroupContent.Input 必须恰好是一个 Input；Prefix/Suffix 用 Some 显式提供，可包含非交互内容或 Button，不能包含其它可聚焦控件或嵌套编辑器。组统一绘制背景、边框、圆角和 focus-within，Input 使用剩余宽度；编辑器状态与 Key 保留。后缀按钮是独立 Tab 停靠点。": "InputGroupContent.Input must be exactly one Input. Supply Prefix/Suffix explicitly with Some; they may contain noninteractive content or Button, but no other focusable controls or nested editors. The group paints the shared background, border, corners, and focus-within appearance; Input takes the remaining width. Editor state and Key are retained. A suffix button is a separate Tab stop.",
    "# Input：单行输入": "# Input: single-line input",
    "用 `Input` 构建单行输入。本页包含可运行Example、完整属性及行为限制。": "Build single-line inputs with `Input`. This page includes a runnable example, all props, and behavior constraints.",
    "| `Value` | `string` | 应用拥有的当前值；行为、范围和零值见本页说明。 |": "| `Value` | `string` | Current application-owned value; see this page for behavior and zero-value semantics. |",
    "| `OnChange` | `func(string)` | 完整新值提案；写回业务变量后下一次构建生效。nil 行为见本页。 |": "| `OnChange` | `func(string)` | Proposes the complete new value; write it to state for the next build. See behavior for nil callbacks. |",
    "| `Selection` | `Option[TextRange]` | 默认未设置，使用内部选区；Some(TextRange) 后为受控 rune 选区。 |": "| `Selection` | `Option[TextRange]` | Unset uses internal selection; Some(TextRange) controls rune-based selection. |",
    "| `OnSelectionChange` | `func(TextRange)` | 完整 rune 选区提案；nil 保留内部选区，受控 Selection 在重建时优先。 |": "| `OnSelectionChange` | `func(TextRange)` | Proposes a complete rune selection. Nil retains internal selection; controlled Selection takes precedence on rebuild. |",
    "| `Placeholder` | `string` | 空 Value 时显示；默认空字符串。 |": "| `Placeholder` | `string` | Displayed for an empty Value; defaults to empty. |",
    "| `Password` | `bool` | 默认 false；true 遮蔽显示并禁止复制/剪切。 |": "| `Password` | `bool` | Defaults to false; true masks text and disables copy/cut. |",
    "| `ShowPasswordToggle` | `bool` | 默认 false；仅 Password=true 生效，保留身份期间记住显示状态。 |": "| `ShowPasswordToggle` | `bool` | Defaults to false; effective only with Password=true. Visibility persists while identity is retained. |",
    "| `ReadOnly` | `bool` | 默认 false；true 禁止编辑和预编辑，保留导航与选择。 |": "| `ReadOnly` | `bool` | Defaults to false; true prevents editing/preedit while preserving navigation and selection. |",
    "| `OnSubmit` | `func()` | Input 的 Enter 回调；ReadOnly 时仍可调用，Disabled 时不可。 |": "| `OnSubmit` | `func()` | Input Enter callback; still available when ReadOnly, unavailable when Disabled. |",
    "Value 是完整受控字符串；不写回 OnChange 提案就拒绝编辑。Selection 用 rune 下标，Some 后必须接纳 OnSelectionChange 才能移动选区。ReadOnly 保留导航、选区、非密码复制与 OnSubmit；Disabled 移除焦点并停止输入。密码即使显示明文也禁止复制/剪切。ShowPasswordToggle 仅在 Password=true 时有效，只改变内部显示，不触发 OnChange。IME 组合阶段不触发 OnChange；提交后才产生值。": "Value is a fully controlled string; rejecting OnChange proposals rejects edits. Selection uses rune indices; controlled selection must accept OnSelectionChange to move. ReadOnly preserves navigation, selection, non-password copy, and OnSubmit; Disabled removes focus and stops input. Passwords cannot be copied/cut even when visible. ShowPasswordToggle only works with Password=true and changes internal visibility without OnChange. IME composition does not call OnChange until text is committed.",
    "# Menu：菜单动作列表": "# Menu: action lists",
    "用 `Menu` 构建菜单动作列表。本页包含可运行Example、完整属性及行为限制。": "Build action lists with `Menu`. This page includes a runnable example, all props, and behavior constraints.",
    "| `Orientation` | `MenuOrientation` | 默认水平（ButtonGroup）或垂直（Menu）；详见本页。 |": "| `Orientation` | `MenuOrientation` | Defaults to vertical; see behavior below. |",
    "| `Items` | `[]MenuItem` | 复制的 TabItem/MenuItem 列表；值非空唯一，允许空列表。 |": "| `Items` | `[]MenuItem` | Copied item list with nonempty unique values; may be empty. |",
    "| `OnAction` | `func(string)` | 每次执行启用菜单项时接收 Value；允许重复执行同一项。 |": "| `OnAction` | `func(string)` | Receives Value whenever an enabled item executes; the same item may execute repeatedly. |",
    "普通布局内的动作列表，默认 MenuVertical，MenuHorizontal 改为水平。Value 是可选受控选择外观；OnAction 对已选中项仍会再次触发。Item.Value 非空唯一。一个菜单贡献一个 Tab 停靠点；对应方向键/Home/End 移动内部当前项，不循环，Enter/Space 执行。无内置弹层、子菜单、分隔线、路由或快捷键字段。长菜单组合 Scroll。": "An action list within normal layout. Defaults to MenuVertical; MenuHorizontal selects horizontal. Value optionally controls selected appearance; OnAction still fires for an already-selected item. Item.Value must be nonempty and unique. One menu contributes one Tab stop. Orientation-specific arrows/Home/End move the internal current item without wrapping; Enter/Space executes. No built-in popup, submenus, separators, routing, or shortcut fields. Compose long menus with Scroll.",
    "# Popover：交互弹层": "# Popover: interactive overlays",
    "用 `Popover` 构建交互弹层。本页包含可运行Example、完整属性及行为限制。": "Build interactive overlays with `Popover`. This page includes a runnable example, all props, and behavior constraints.",
    "| `Open` | `bool` | 受控打开状态，默认 false。 |": "| `Open` | `bool` | Controlled open state, default false. |",
    "| `Placement` | `OverlayPlacement` | 默认 OverlayBottomStart；共八个方向/对齐枚举，见参考。 |": "| `Placement` | `OverlayPlacement` | Defaults to OverlayBottomStart; eight placement/alignment values are available. |",
    "| `Offset` | `MetricValue` | Scroll/VirtualList 为可选受控 Point；浮层为可选语义的 MetricValue 间距。 |": "| `Offset` | `MetricValue` | Optional literal/token spacing for the overlay. |",
    "| `OnOpenChange` | `func(bool)` | 打开/关闭布尔提案；nil 不改变 Open。 |": "| `OnOpenChange` | `func(bool)` | Proposes opening/closing; nil leaves Open unchanged. |",
    "接受 anchor、content 两个 View；Open 必须由应用接纳 OnOpenChange。触发器指针/Enter/Space、顶层 Escape 或外部主键点击会提案。弹层位于窗口级，自动翻转/钳制；Tab 可转入内容，关闭时恢复仍存活触发器焦点。没有 Disabled 字段；禁用 anchor 子组件不等于禁用 Popover 宿主。无模态、焦点陷阱、箭头或动画。": "Accepts anchor and content Views. The app must accept OnOpenChange proposals to update Open. Pointer/Enter/Space on the trigger, topmost Escape, or an outside primary click proposes a change. Window-level overlays flip/clamp automatically. Tab can enter content; closing restores a surviving trigger's focus. No Disabled field; disabling an anchor child does not disable the Popover host. No modal behavior, focus trap, arrow, or animation.",
    "# ProgressBar：进度条": "# ProgressBar: progress indicators",
    "用 `ProgressBar` 构建进度条。本页包含可运行Example、完整属性及行为限制。": "Build progress indicators with `ProgressBar`. This page includes a runnable example, all props, and behavior constraints.",
    "| `Value` | `float32` | 应用拥有的当前值；行为、范围和零值见本页说明。 |": "| `Value` | `float32` | Current application-owned value; see this page for its range and zero-value behavior. |",
    "Value 为 0..1 比例，不是百分数；越界钳制，NaN/-Inf 为空，+Inf 为满。无事件和内部进度计时。Background 改轨道色，Text.Color 改填充色；默认 160×12，轨道高 8。没有不确定进度、分段、竖向或动画模式。": "Value is a 0..1 ratio, not a percentage. Out-of-range values clamp; NaN/-Inf are empty and +Inf is full. No events or internal progress timer. Background controls the track; Text.Color controls the fill. Default size is 160×12 with an 8-unit track. No indeterminate, segmented, vertical, or animated modes.",
    "# Radio：单选按钮": "# Radio: radio buttons",
    "用 `Radio` 构建单选按钮。本页包含可运行Example、完整属性及行为限制。": "Build radio buttons with `Radio`. This page includes a runnable example, all props, and behavior constraints.",
    "| `Selected` | `bool` | 当前是否选中，默认 false；与共享业务值比较得到。 |": "| `Selected` | `bool` | Selected state, default false; derive it from a shared application value. |",
    "| `OnSelect` | `func()` | 未选中 Radio 激活时调用，无参数；由应用更新共享选择值。 |": "| `OnSelect` | `func()` | Called without arguments when an enabled unselected Radio activates; update the shared value in the app. |",
    "Selected 是权威值。启用且未选中时，指针或 Space 调用 OnSelect；已选中不会反选，也不回调。互斥由应用共享业务变量实现，没有 RadioGroup。每个 Radio 保留自己的焦点停靠点。": "Selected is authoritative. Pointer or Space calls OnSelect only when enabled and unselected; a selected item neither deselects nor calls back. Use shared application state for exclusivity; there is no RadioGroup. Every Radio keeps its own focus stop.",
    "# Scroll：滚动容器": "# Scroll: scrolling containers",
    "用 `Scroll` 构建滚动容器。本页包含可运行Example、完整属性及行为限制。": "Build scrolling containers with `Scroll`. This page includes a runnable example, all props, and behavior constraints.",
    "| `Axis` | `ScrollAxis` | 默认 ScrollVertical；启用轴无界测量子项。 |": "| `Axis` | `ScrollAxis` | Defaults to ScrollVertical; children are measured without bounds on enabled axes. |",
    "| `InitialOffset` | `Option[Point]` | 可选首次挂载偏移；后续修改不会重置内部位置。 |": "| `InitialOffset` | `Option[Point]` | Optional initial mount offset; later changes do not reset internal position. |",
    "| `Offset` | `Option[Point]` | Scroll/VirtualList 为可选受控 Point；浮层为可选语义的 MetricValue 间距。 |": "| `Offset` | `Option[Point]` | Optional controlled scroll position. |",
    "| `Scrollbar` | `ScrollbarPolicy` | 默认 ScrollbarAuto；Always 始终显示，Hidden 隐藏轨道但仍可滚动。 |": "| `Scrollbar` | `ScrollbarPolicy` | Defaults to ScrollbarAuto; Always always shows, Hidden hides the track but permits scrolling. |",
    "| `OnScroll` | `func(Point)` | 完整 Point 提案；未设置 Offset 时内部滚动不依赖此回调。 |": "| `OnScroll` | `func(Point)` | Proposes a complete offset; uncontrolled internal scrolling does not depend on this callback. |",
    "恰好一个 child。Axis 默认 ScrollVertical，可选 ScrollHorizontal/ScrollBoth；启用轴对子项无上限测量，因此视口需要确定大小。Offset 未设置时保留内部偏移，InitialOffset 只首次挂载使用；Some Offset 后 OnScroll 仅提案。ScrollbarHidden 仍允许滚轮。嵌套滚动在边界传递剩余量。没有惯性/平滑滚动；普通 Scroll 挂载全部内容。": "Exactly one child. Axis defaults to ScrollVertical; ScrollHorizontal/ScrollBoth are available. Children are measured unbounded on enabled axes, so the viewport needs definite dimensions. Unset Offset retains internal state; InitialOffset applies only at first mount. With Some Offset, OnScroll only proposes changes. ScrollbarHidden still permits wheel scrolling. Nested scrollers pass remaining delta at boundaries. No inertia or smooth scrolling; ordinary Scroll mounts all content.",
    "# Select：下拉选择": "# Select: dropdown selection",
    "用 `Select` 构建下拉选择。本页包含可运行Example、完整属性及行为限制。": "Build dropdowns with `Select`. This page includes a runnable example, all props, and behavior constraints.",
    "| `Options` | `[]SelectOption` | 复制的 SelectOption 列表；值非空唯一，上限 4096。 |": "| `Options` | `[]SelectOption` | Copied option list; nonempty unique values, at most 4096 items. |",
    "Options 的 Value 必须非空且唯一，作为重排身份。支持空选项列表和未匹配 Value。nil OnChange 仍可打开、浏览、关闭，只不接纳选择。支持 Enter/Space、方向键、Home/End、Escape、Tab；禁用选项跳过。弹层逃逸祖先 Scroll 裁剪，并在窗口边缘翻转/钳制。不是原生系统选择框，不虚拟化，最多 4096 项。": "Option Value must be nonempty and unique for reorder identity. Empty lists and unmatched Value are supported. Nil OnChange still permits opening, browsing, and closing without accepting a selection. Enter/Space, arrows, Home/End, Escape, and Tab are supported; disabled options are skipped. The overlay escapes ancestor Scroll clipping and flips/clamps at window edges. Not a native system selector or virtualized list; at most 4096 items.",
    "# Slider：滑块": "# Slider: numeric sliders",
    "用 `Slider` 构建滑块。本页包含可运行Example、完整属性及行为限制。": "Build numeric sliders with `Slider`. This page includes a runnable example, all props, and behavior constraints.",
    "| `Min` | `float32` | 下界；与 Max 同为 0 时使用默认 0..100，否则按字面值；须有限且小于 Max。 |": "| `Min` | `float32` | Lower bound; if both bounds are 0, uses 0..100. Otherwise literal, finite, and less than Max. |",
    "| `Max` | `float32` | 上界；与 Min 同为 0 时默认 100，否则按字面值；须有限且大于 Min。 |": "| `Max` | `float32` | Upper bound; if both bounds are 0, defaults to 100. Otherwise literal, finite, and greater than Min. |",
    "| `Step` | `float32` | 以 Min 为起点的步长；非正或非有限时默认 1，端点仍可到达。 |": "| `Step` | `float32` | Steps from Min; nonpositive or nonfinite values default to 1. Endpoints remain reachable. |",
    "| `OnChange` | `func(float32)` | 完整新值提案；写回业务变量后下一次构建生效。nil 行为见本页。 |": "| `OnChange` | `func(float32)` | Proposes the complete new value; write it to state for the next build. See behavior for nil callbacks. |",
    "Min 和 Max 同为零表示 0..100；Step 非正或非有限时回退 1。其它边界按字面使用，非有限边界或 Min>=Max 使控件不可交互、不可聚焦。Value 超范围被钳制；NaN/-Inf 显示 Min，+Inf 显示 Max。提案以 Min 为起点对齐步长，端点仍可到达。支持轨道点击、捕获拖动、方向键、Home/End。": "Both bounds at zero mean 0..100; nonpositive/nonfinite Step falls back to 1. Other bounds are literal. Nonfinite bounds or Min>=Max disable interaction and focus. Value clamps; NaN/-Inf display Min and +Inf displays Max. Proposals align steps from Min while preserving endpoint access. Supports track clicks, captured dragging, arrows, Home, and End.",
    "# Tabs：标签页选择器": "# Tabs: tab selection",
    "用 `Tabs` 构建标签页选择器。本页包含可运行Example、完整属性及行为限制。": "Build tab selectors with `Tabs`. This page includes a runnable example, all props, and behavior constraints.",
    "| `Items` | `[]TabItem` | 复制的 TabItem/MenuItem 列表；值非空唯一，允许空列表。 |": "| `Items` | `[]TabItem` | Copied item list with nonempty unique values; may be empty. |",
    "只渲染横向标签，内容面板由应用依据 Value 构建。Items.Value 必须非空且唯一；空/未匹配 Value 不显示选中指示。左右/Home/End 移动内部当前项，Enter/Space 才提案；已选中或 Disabled 项不回调。没有 TabPanel、垂直、滚动、关闭或拖拽排序 API。": "Renders horizontal tabs only; the app builds content based on Value. Items.Value must be nonempty and unique. Empty/unmatched Value shows no selected indicator. Left/right/Home/End move the internal current item; Enter/Space proposes selection. Selected or disabled items do not call back. No TabPanel, vertical tabs, scrolling, closing, or drag-reordering API.",
    "# Text：文本与 Label": "# Text: text and Label",
    "用 `Text` 构建文本与 Label。本页包含可运行Example、完整属性及行为限制。": "Build text with `Text` and `Label`. This page includes a runnable example, all props, and behavior constraints.",
    "| `Wrap` | `TextWrap` | 默认 TextNoWrap；TextWrapWords 简单按词换行。 |": "| `Wrap` | `TextWrap` | Defaults to TextNoWrap; TextWrapWords uses simple word wrapping. |",
    "| `MaxLines` | `int` | 正数限制可见行数；0 不限制，负数使构建失败。 |": "| `MaxLines` | `int` | Positive values limit visible lines; 0 is unlimited, negative values fail the build. |",
    "Label(value) 等价于默认 Text；TextNoWrap 为零值。TextWrapWords 使用简单分词换行，合并空白，并非完整 Unicode 换行。MaxLines 为正时限制行数。无效 UTF-8 替换为 U+FFFD。文本本身不是可选择的编辑器；中文依赖字体覆盖。": "Label(value) is default Text; TextNoWrap is the zero value. TextWrapWords uses simple word splitting and collapses whitespace, not full Unicode line breaking. Positive MaxLines limits lines. Invalid UTF-8 becomes U+FFFD. Text is not a selectable editor; CJK display depends on font coverage.",
    "# Textarea：多行输入": "# Textarea: multiline input",
    "用 `Textarea` 构建多行输入。本页包含可运行Example、完整属性及行为限制。": "Build multiline inputs with `Textarea`. This page includes a runnable example, all props, and behavior constraints.",
    "遵循 Input 的受控值与 rune 选区规则。Enter 在可编辑状态插入换行，无 OnSubmit、Password 或密码按钮。Wrap 默认 TextNoWrap，可选 TextWrapWords。保留内部光标滚动和撤销历史；ReadOnly 仍允许选择、复制和滚动。不是富文本编辑器，不支持 grapheme 编辑或 RTL 排版。": "Uses Input's controlled-value and rune-selection rules. Enter inserts a newline when editable; no OnSubmit, Password, or password toggle. Wrap defaults to TextNoWrap, with TextWrapWords available. Retains internal caret scrolling and undo history. ReadOnly still allows selection, copy, and scrolling. Not a rich-text editor; no grapheme editing or RTL layout.",
    "# ToggleSwitch：开关": "# ToggleSwitch: switches",
    "用 `ToggleSwitch` 构建开关。本页包含可运行Example、完整属性及行为限制。": "Build switches with `ToggleSwitch`. This page includes a runnable example, all props, and behavior constraints.",
    "Checked / OnChange 与 Checkbox 相同，通过指针或 Space 提案。默认尺寸由主题指标决定（40×24 逻辑单位）。没有内置文字标签，可与 Text/Label 组合；nil 回调仍有交互外观。": "Checked/OnChange follow Checkbox, with pointer or Space proposals. Theme metrics set the default size (40×24 logical units). No built-in text label; compose Text/Label. Nil callbacks retain interaction appearance.",
    "# Tooltip：提示浮层": "# Tooltip: hints",
    "用 `Tooltip` 构建提示浮层。本页包含可运行Example、完整属性及行为限制。": "Build hints with `Tooltip`. This page includes a runnable example, all props, and behavior constraints.",
    "| `Delay` | `time.Duration` | time.Duration；0 使用 500ms 默认延迟，负数无效。 |": "| `Delay` | `time.Duration` | 0 selects the default 500ms delay; negative values are invalid. |",
    "接受 anchor、content；鼠标悬停或键盘焦点启动 Delay，离开且失焦后关闭。Delay=0 表示默认 500ms，不是立即出现。Disabled 抑制提示。内容不可交互，不抢焦点、不阻挡指针；需要按钮等交互内容时使用 Popover。由事件/截止时间驱动，无需应用计时循环。": "Accepts anchor and content. Hover or keyboard focus starts Delay; leaving and losing focus closes it. Delay=0 means 500ms, not immediate display. Disabled suppresses the hint. Content is noninteractive, takes no focus, and does not block pointers; use Popover for interactive content. Driven by events/deadlines, with no application timer loop.",
    "# VirtualList：固定行高虚拟列表": "# VirtualList: fixed-height virtual lists",
    "用 `VirtualList` 构建固定行高虚拟列表。本页包含可运行Example、完整属性及行为限制。": "Build fixed-height virtual lists with `VirtualList`. This page includes a runnable example, all props, and behavior constraints.",
    "| `Count` | `int` | 快照条目数；0 为空，最大 MaxVirtualListItems。 |": "| `Count` | `int` | Snapshot item count; 0 is empty, maximum MaxVirtualListItems. |",
    "| `Version` | `uint64` | 快照版本 uint64；内容/键改变时递增，不能在 Build 内修改。 |": "| `Version` | `uint64` | Increment when content/keys change; do not modify inside Build. |",
    "| `RowHeight` | `float32` | 每行完整有限正逻辑高度，包括间距。 |": "| `RowHeight` | `float32` | Complete finite positive logical row height, including spacing. |",
    "| `Overscan` | `int` | 视口外保留的行数，0..MaxVirtualListOverscan。 |": "| `Overscan` | `int` | Retained rows outside the viewport, 0..MaxVirtualListOverscan. |",
    "| `ItemKey` | `func(index int) string` | 按索引返回全列表稳定、非空、唯一键；UI 线程纯函数。 |": "| `ItemKey` | `func(index int) string` | Returns stable nonempty keys unique across the full list; a pure UI-thread function. |",
    "| `Build` | `func(index int) View` | 按索引构建行 View；只为挂载窗口构建，不可有副作用。 |": "| `Build` | `func(index int) View` | Builds a row by index only in the mounted range; must have no side effects. |",
    "仅垂直固定行高；必须给出有限的像素 Height。RowHeight 包括间距，行被限制为该高度并裁剪。Count 上限 MaxVirtualListItems=10,000,000，Overscan 上限 MaxVirtualListOverscan=256。ItemKey 全列表非空唯一；Count/Version 是不可变快照，内容或键变更必须递增 Version。回调在 UI 线程且应纯粹。离开 overscan 即卸载，焦点、IME、编辑和弹层状态不恢复；业务值放在列表外。非受控滚动按首个幸存可见键锚定；受控 Offset 优先。": "Vertical fixed-height rows only; supply a finite pixel Height. RowHeight includes spacing; each row is constrained and clipped to it. MaxVirtualListItems=10,000,000; MaxVirtualListOverscan=256. ItemKey must be nonempty and globally unique within the list. Count/Version describe an immutable snapshot; increment Version for content/key changes. Callbacks run on the UI thread and must be pure. Leaving overscan unmounts rows and discards focus, IME, editor, and overlay state; keep business values outside the list. Uncontrolled scrolling anchors to the first surviving visible key; controlled Offset takes precedence.",
    "# 运行完整Example": "# Running complete examples",
    "每个组件页引用本站 `docs/examples/<组件>/main.go` 的同一份代码，避免展示代码与编译代码分叉。界面 API 仅使用根包与官方 icon 资源；Image/Avatar Example自行创建像素，不需要外部文件。": "Component pages include the same `docs/examples/<component>/main.go` files used by compilation checks. UI code uses only the root package and official icons. Image/Avatar examples generate pixels themselves and need no external files.",
    "在任意空目录执行快速开始的 `go mod init`、`go get`，把所选Example复制为 `main.go` 后 `go run .`。这些是原生窗口程序，不是在浏览器中运行的 Go。": "In an empty directory, follow the quick start's `go mod init` and `go get`, copy an example to `main.go`, and run `go run .`. These are native window programs, not Go running in the browser.",
    "- [多窗口与跨窗口状态更新](/guide/multi-window#完整Example)": "- [Multiple windows and shared state updates](/guide/multi-window#complete-example)",
    "- [主窗口与子窗口控制](/api/windows#完整操作Example)": "- [Main and child window controls](/api/windows#complete-window-controls-example)",
    "- [Box 完整Example](/components/box#完整Example)": "- [Box complete example](/components/box#complete-example)",
    "- [Text 完整Example](/components/text#完整Example)": "- [Text complete example](/components/text#complete-example)",
    "- [Button 完整Example](/components/button#完整Example)": "- [Button complete example](/components/button#complete-example)",
    "- [ButtonGroup 完整Example](/components/button-group#完整Example)": "- [ButtonGroup complete example](/components/button-group#complete-example)",
    "- [InputGroup 完整Example](/components/input-group#完整Example)": "- [InputGroup complete example](/components/input-group#complete-example)",
    "- [Input 完整Example](/components/input#完整Example)": "- [Input complete example](/components/input#complete-example)",
    "- [Textarea 完整Example](/components/textarea#完整Example)": "- [Textarea complete example](/components/textarea#complete-example)",
    "- [Checkbox 完整Example](/components/checkbox#完整Example)": "- [Checkbox complete example](/components/checkbox#complete-example)",
    "- [Radio 完整Example](/components/radio#完整Example)": "- [Radio complete example](/components/radio#complete-example)",
    "- [ToggleSwitch 完整Example](/components/toggle-switch#完整Example)": "- [ToggleSwitch complete example](/components/toggle-switch#complete-example)",
    "- [Slider 完整Example](/components/slider#完整Example)": "- [Slider complete example](/components/slider#complete-example)",
    "- [Select 完整Example](/components/select#完整Example)": "- [Select complete example](/components/select#complete-example)",
    "- [Tabs 完整Example](/components/tabs#完整Example)": "- [Tabs complete example](/components/tabs#complete-example)",
    "- [Menu 完整Example](/components/menu#完整Example)": "- [Menu complete example](/components/menu#complete-example)",
    "- [Scroll 完整Example](/components/scroll#完整Example)": "- [Scroll complete example](/components/scroll#complete-example)",
    "- [VirtualList 完整Example](/components/virtual-list#完整Example)": "- [VirtualList complete example](/components/virtual-list#complete-example)",
    "- [Popover 完整Example](/components/popover#完整Example)": "- [Popover complete example](/components/popover#complete-example)",
    "- [Tooltip 完整Example](/components/tooltip#完整Example)": "- [Tooltip complete example](/components/tooltip#complete-example)",
    "- [Icon 完整Example](/components/icon#完整Example)": "- [Icon complete example](/components/icon#complete-example)",
    "- [Image 完整Example](/components/image#完整Example)": "- [Image complete example](/components/image#complete-example)",
    "- [Avatar 完整Example](/components/avatar#完整Example)": "- [Avatar complete example](/components/avatar#complete-example)",
    "- [Badge 完整Example](/components/badge#完整Example)": "- [Badge complete example](/components/badge#complete-example)",
    "- [ProgressBar 完整Example](/components/progress-bar#完整Example)": "- [ProgressBar complete example](/components/progress-bar#complete-example)",
    "维护者在网站根目录执行 `python scripts/check-docs.py <dxui源码路径>`，会逐个编译完整Example并核对公开 API 快照。编译成功不能替代本机窗口交互验收。": "Maintainers can run `python scripts/check-docs.py <dxui-source>` in the website root to compile every complete example and compare the public API snapshot. Successful compilation does not replace native interaction testing.",
    "# 后台任务与生命周期": "# Background tasks and lifecycle",
    "耗时计算放进 goroutine，完成后通过 `App.Update` 更新界面，避免窗口卡住。": "Run expensive computations in a goroutine and update the UI through `App.Update` when finished, keeping the window responsive.",
    "## 1. 运行Example": "## 1. Run the example",
    "点击 **Compute**，完成后会显示计算结果。计算期间按钮禁用；任务较快时，可能看不到中间的 Computing 提示。": "Click **Compute** to display the result. The button is disabled during calculation; fast tasks may finish before the Computing message is visible.",
    "## 2. 按三个步骤处理任务": "## 2. Handle tasks in three steps",
    "1. **点击时准备数据**：设置 `busy`，把计算需要的输入放进局部变量 `n`。": "1. **Prepare input on click**: Set `busy` and copy the required input into local variable `n`.",
    "2. **后台计算**：goroutine 只使用准备好的输入，算出 `result`。": "2. **Compute in the background**: The goroutine uses only the prepared input to produce `result`.",
    "3. **回到界面更新**：在 `app.Update` 的闭包中修改 `message` 和 `busy`。": "3. **Update the UI**: Change `message` and `busy` inside the `app.Update` closure.",
    "不要在 goroutine 中直接修改界面正在读取的变量。`Assign` 只是赋值函数，不能代替 `Update`。": "Do not directly change variables read by the UI from a goroutine. `Assign` is only an assignment function, not a replacement for `Update`.",
    "`Update` 返回 nil 说明工作已排队，不代表闭包已经执行。应用关闭后提交会失败，因此Example检查并处理返回的错误。": "A nil result from `Update` means the work was queued, not executed. Submission fails after shutdown, so the example handles the returned error.",
    "## 3. 关闭时停止任务": "## 3. Stop work during shutdown",
    "Example使用 `context.WithCancel`。系统请求关闭窗口时，`OnCloseRequest` 先调用 `cancel()` 停止后台工作，再调用 `app.Close()` 退出。": "The example uses `context.WithCancel`. On a system close request, `OnCloseRequest` calls `cancel()` to stop background work, then `app.Close()` to exit.",
    "设置了 `OnCloseRequest` 后，需要显式调用 `Close` 才会接受关闭。`Run` 返回后也调用一次 `cancel()`，覆盖其它退出路径。": "When OnCloseRequest is set, explicitly call Close to accept closure. Calling cancel() again after Run returns covers other exit paths.",
    "试着增加计算量，并在任务执行中关闭窗口。后台任务应响应取消，不再继续提交结果。": "Try increasing the workload and closing the window during computation. The task should honor cancellation instead of submitting further results.",
    "::: details 应用启动、关闭和错误回调": "::: details Startup, shutdown, and error callbacks",
    "NewApp 不加载原生窗口；配置/字体/主题/原生启动错误由 Run 返回。OnShown 仅在完整第一帧呈现并成功显示后调用一次，适合启动依赖窗口已就绪的工作。不要把 root builder 当作首次显示通知。": "NewApp does not create native windows. Run returns configuration, font, theme, and native startup errors. OnShown runs once after the complete first frame is presented and shown; use it for work requiring a ready window, not the root builder.",
    "主窗口默认系统关闭请求会结束应用，同时关闭全部子窗口。如果设置 `OnCloseRequest func(*App)`，你必须在接受请求时调用 Close；不调用就是保持窗口打开，可用于未保存提示。App.Close 幂等且允许跨 goroutine，Run 前调用为空操作。Window.Close 只关闭目标子窗口，见[多窗口教程](./multi-window)。": "By default, a main-window system close request ends the application and closes all children. With `OnCloseRequest func(*App)`, call Close to accept; returning without it keeps the window open, useful for unsaved changes. App.Close is idempotent, concurrent-safe, and a no-op before Run. Window.Close affects only its child; see [multiple windows](./multi-window).",
    "OnError 在 UI 线程观察可恢复的构建/回调失败；为 nil 时错误终止 Run 并返回。错误处理器应记录或调整导致问题的业务状态，不能依赖一个持续出错的 builder 自动恢复。零 View、非法条目键等使树更新事务失败，保留上一个有效帧；首次构建没有旧帧可保留。不要用 OnError 忽略启动失败。": "OnError observes recoverable build/callback failures on the UI thread; nil ends Run with the error. Log or correct the problematic state rather than relying on a persistently failing builder to recover. Zero Views and invalid item keys fail tree-update transactions, retaining the previous valid frame; the first build has no previous frame. Do not use OnError to ignore startup failures.",
    "快捷键、剪贴板和生命周期参数见[应用 API](/api/application)。": "See [application APIs](/api/application) for shortcuts, clipboard, and lifecycle parameters.",
    "下一步：[打开多个窗口](./multi-window)。": "Next: [Open multiple windows](./multi-window).",
    "# 构建一个表单": "# Building a form",
    "这一节做一个设置表单：输入名称、选择语言、勾选条款，最后点击保存。": "Build a settings form: enter a name, select a language, accept the terms, and save.",
    "## 2. 按顺序试一遍": "## 2. Try it in order",
    "1. 不填名称，观察 **Save** 按钮不可用。": "1. Leave the name empty and observe that **Save** is disabled.",
    "2. 输入名称，选择语言，再勾选 **Accept terms**。": "2. Enter a name, choose a language, and check **Accept terms**.",
    "3. 点击 **Save**，下方显示 `Saved: ...`；在名称输入框中按 Enter 也能提交。": "3. Click **Save** to display `Saved: ...`. Pressing Enter in the name input also submits.",
    "4. 清空名称或取消勾选，保存按钮再次变为不可用。": "4. Clear the name or uncheck the terms; Save becomes disabled again.",
    "这里的“保存”只更新界面文字，没有写入文件。": "Saving in this example updates text only; it does not write a file.",
    "## 3. 看懂表单逻辑": "## 3. Understand the form logic",
    "| 代码 | 作用 |": "| Code | Purpose |",
    "| `name`、`language`、`accepted` | 保存三个控件的值。 |": "| `name`, `language`, `accepted` | Store the three control values. |",
    "| `OnChange: dxui.Assign(...)` | 把操作结果写回对应变量。 |": "| `OnChange: dxui.Assign(...)` | Writes changes into the corresponding variable. |",
    "| `valid()` | 检查名称不为空且已勾选条款。 |": "| `valid()` | Checks for a nonempty name and accepted terms. |",
    "| `Disabled: !valid()` | 未满足条件时禁用保存按钮。 |": "| `Disabled: !valid()` | Disables Save while the conditions are unmet. |",
    "| `submit()` | 再检查一次条件，然后更新提示文字。 |": "| `submit()` | Rechecks conditions, then updates the message. |",
    "按钮和输入框的 Enter 都调用 `submit`，所以校验要写在这个函数里，不能只依赖按钮禁用。": "Both the button and input Enter action call submit, so validate inside that function instead of relying only on the disabled button.",
    "试着给 `valid()` 增加“名称至少两个字符”的条件，再分别用按钮和 Enter 提交。": "Try requiring at least two characters in valid(), then submit through both the button and Enter.",
    "需要更多控件配置时，查看 [Input](/components/input)、[Select](/components/select) 和 [Checkbox](/components/checkbox)。": "For additional configuration, see [Input](/components/input), [Select](/components/select), and [Checkbox](/components/checkbox).",
    "下一步：[执行后台任务](./async)。": "Next: [Run background tasks](./async).",
    "# 第一个窗口": "# Your first window",
    "这一节先运行一个带文字和按钮的桌面窗口。需要 Go 1.25 或更新版本，以及 Windows、macOS 或 Linux 桌面环境。": "Run a desktop window with text and a button. You need Go 1.25 or newer and a Windows, macOS, or Linux desktop environment.",
    "## 1. 创建项目": "## 1. Create a project",
    "## 2. 复制代码": "## 2. Copy the code",
    "在项目目录新建 `main.go`，复制下面的完整代码：": "Create `main.go` in the project directory and copy this complete program:",
    "## 3. 运行": "## 3. Run it",
    "在项目目录执行：": "In the project directory, run:",
    "看到 **Hello dxui** 窗口就成功了。点击 **Close** 关闭窗口。": "If you see the **Hello dxui** window, it worked. Click **Close** to close it.",
    "## 这段代码做了什么": "## What the code does",
    "| `NewApp` | 设置窗口标题、宽度和高度。 |": "| `NewApp` | Configures the window title, width, and height. |",
    "| `app.Run` | 打开窗口，运行到应用关闭。 |": "| `app.Run` | Opens the window and runs until the application closes. |",
    "| `Box` | 把里面的控件排列在一起，默认竖排。 |": "| `Box` | Arranges its children vertically by default. |",
    "| `Label` / `TextButton` | 显示文字 / 显示按钮。 |": "| `Label` / `TextButton` | Displays text / a button. |",
    "| `OnPress` | 按钮点击时执行的函数。 |": "| `OnPress` | Function called when the button is activated. |",
    "试着修改 `Title` 和 `Label` 中的文字，再运行一次。`Run` 要在 `main` 中调用，不要放进 goroutine。": "Try changing Title and the Label text, then run again. Call Run from main, not from a goroutine.",
    "::: details 可选：构建可执行文件、关闭 CGO": "::: details Optional: build an executable without CGO",
    "保留 `go.mod` 和 `go.sum`，记录项目使用的依赖版本。": "Keep `go.mod` and `go.sum` to record dependency versions.",
    "打不开窗口时，先查看终端错误，再看[排错指南](./troubleshooting)。": "If the window does not open, inspect the terminal error, then consult [troubleshooting](./troubleshooting).",
    "下一步：[让按钮改变界面](./state)。": "Next: [Make a button update the UI](./state).",
    "# 学习路线": "# Learning path",
    "会写 Go 就可以开始。先打开[第一个窗口](./getting-started)，复制完整代码，在本地运行 `go run .`。": "If you know Go, you are ready. Start with [your first window](./getting-started), copy the complete program, and run `go run .` locally.",
    "## 入门：先学这三步": "## Start with these three steps",
    "1. [运行窗口](./getting-started)：安装 DXUI，看到第一个桌面窗口。": "1. [Run a window](./getting-started): Install DXUI and open your first desktop window.",
    "2. [添加交互](./state)：点击按钮改变数字，输入文字并保存。": "2. [Add interaction](./state): Change a number with a button and retain text input.",
    "3. [排列界面](./layout)：把控件横排或竖排，让界面适应窗口大小。": "3. [Arrange the UI](./layout): Place controls horizontally or vertically and adapt to window size.",
    "## 接着做一个小应用": "## Build a small application",
    "学习[样式与主题](./style)，调整颜色、间距和明暗主题；再完成[表单Example](./forms)，练习输入、校验和提交。": "Explore [styles and themes](./style) for colors, spacing, and light/dark appearance. Then complete the [form example](./forms) to practice input, validation, and submission.",
    "## 用到时再看": "## Read these when needed",
    "- 耗时任务：[后台任务](./async)。": "- Expensive work: [Background tasks](./async).",
    "- 打开更多窗口：[多窗口](./multi-window)。": "- Additional windows: [Multiple windows](./multi-window).",
    "- 遇到问题：[排错指南](./troubleshooting)。": "- Problems: [Troubleshooting](./troubleshooting).",
    "- 找控件和Example：[组件参考](/components/)；查方法和参数：[API 索引](/api/)。": "- Controls and examples: [Component reference](/components/). Methods and parameters: [API index](/api/).",
    "# 布局与响应式": "# Layout and responsive UI",
    "这一节把导航和内容排在一起，并让它们随窗口宽度切换排列方式。": "Arrange navigation and content together, then switch their direction based on window width.",
    "拖窄窗口：宽度小于 600 时，左右排列变成上下排列。": "Narrow the window: below 600 units, the horizontal arrangement becomes vertical.",
    "## 2. 用 Box 排列控件": "## 2. Arrange controls with Box",
    "| 配置 | 效果 |": "| Configuration | Effect |",
    "| `Direction: dxui.Horizontal` | 从左到右排列。 |": "| `Direction: dxui.Horizontal` | Arranges left to right. |",
    "| `Direction: dxui.Vertical` | 从上到下排列，也是默认值。 |": "| `Direction: dxui.Vertical` | Arranges top to bottom; the default. |",
    "| `Gap: 16` | 控件之间留 16 个逻辑单位。 |": "| `Gap: 16` | Leaves 16 logical units between children. |",
    "| `Padding: dxui.Padding(24)` | 容器四周留 24 个逻辑单位。 |": "| `Padding: dxui.Padding(24)` | Leaves 24 logical units around the container interior. |",
    "`Gap` 放在 `BoxProps` 中，`Padding` 放在它的 `Style` 中。逻辑单位会随屏幕缩放，不等同于物理像素。": "Gap belongs in BoxProps; Padding belongs in its Style. Logical units scale with the screen and are not physical pixels.",
    "## 3. 分配宽度和高度": "## 3. Allocate width and height",
    "Example中的导航使用 `Width: dxui.Px(160)` 固定宽度，配合 `Shrink: dxui.NoShrink()` 防止被挤小；内容区使用 `Grow: 1` 占用排列方向上的剩余空间。": "The navigation uses `Width: dxui.Px(160)` and `Shrink: dxui.NoShrink()` to keep its width. The content uses `Grow: 1` to occupy remaining space along the layout direction.",
    "`RunResponsive` 提供当前窗口的 `ctx.Width` 和 `ctx.Height`。把它们设为根容器尺寸，再根据 `ctx.Width` 选择横排或竖排。": "RunResponsive supplies the current ctx.Width and ctx.Height. Use them for the root container size, then choose horizontal or vertical based on ctx.Width.",
    "试着把切换条件中的 `600` 改成 `800`，观察变化。": "Try changing the breakpoint from 600 to 800 and observe the result.",
    "## 内容太多时怎么办": "## When content does not fit",
    "内容超出容器不会自动滚动。使用 [Scroll](/components/scroll)，并为它设置明确的高度；大量固定高度的行使用 [VirtualList](/components/virtual-list)。": "Overflow does not scroll automatically. Use [Scroll](/components/scroll) with a definite height. For many fixed-height rows, use [VirtualList](/components/virtual-list).",
    "::: details 更多尺寸、定位和裁剪规则": "::: details More sizing, positioning, and clipping rules",
    "| 写法 | 含义与约束 |": "| Setting | Meaning and constraints |",
    "| 未设置 Width/Height | auto，由内容和父约束决定。 |": "| Unset Width/Height | Auto, determined by content and parent constraints. |",
    "| Px(n) | 非负固定逻辑尺寸。 |": "| Px(n) | Nonnegative fixed logical size. |",
    "| Percent(50) | 父级该轴尺寸确定时取一半；无法确定时按 auto。 |": "| Percent(50) | Half of a definite parent axis; otherwise auto. |",
    "| Fill() | Percent(100)，也依赖父级确定尺寸。 |": "| Fill() | Percent(100), also requiring a definite parent size. |",
    "| Grow: 1 | 按比例分配主轴剩余空间。 |": "| Grow: 1 | Proportional allocation of remaining main-axis space. |",
    "| Shrink 未设置 | 默认 1，空间不足时可缩小。 |": "| Unset Shrink | Defaults to 1; may shrink when space is insufficient. |",
    "| Shrink: NoShrink() | 显式 0，不参与缩小；可能导致溢出。 |": "| Shrink: NoShrink() | Explicit 0; prevents shrinking and may overflow. |",
    "| Basis | flex 基准尺寸；支持 auto、Px、Percent。 |": "| Basis | Flex base size; supports auto, Px, and Percent. |",
    "| MinWidth/MaxWidth 等 | 限制最终尺寸；最大值小于最小值时提升到最小值。 |": "| MinWidth/MaxWidth, etc. | Bound final dimensions; a maximum below the minimum is raised to the minimum. |",
    "不要在父级高度由子内容决定时，期望子项 Fill 自动撑满窗口。需要填满窗口时用响应式上下文给根明确宽高。": "A child Fill cannot automatically fill the window when the parent height depends on its children. Give the root explicit responsive dimensions when it must fill the window.",
    "### 滚动不是自动产生的": "### Scrolling is explicit",
    "溢出内容默认不滚动。使用 Scroll 并给视口确定的高度；它在允许滚动的轴上对内容无界测量。完整例子见 [Scroll](/components/scroll)。需要几千或更多行时使用 [VirtualList](/components/virtual-list)，并理解它的固定高度与卸载规则。": "Overflow does not scroll by default. Give Scroll a definite viewport height; it measures content unbounded on enabled axes. See [Scroll](/components/scroll). For thousands of rows, use [VirtualList](/components/virtual-list) with its fixed-height and unmounting rules.",
    "### 绝对定位、裁剪与层叠": "### Absolute positioning, clipping, and stacking",
    "`Style.Position=PositionAbsolute` 配合 `Insets{Top, Right, Bottom, Left}`。绝对定位子项不参加 flex 尺寸分配和 Gap 计数。`ZIndex` 只改变同级绘制/命中顺序，不改变布局和 Tab 源顺序。": "Use `Style.Position=PositionAbsolute` with `Insets{Top, Right, Bottom, Left}`. Absolute children do not participate in flex allocation or Gap counting. ZIndex changes only sibling paint/hit order, not layout or source-order Tab navigation.",
    "`OverflowClip` 是矩形裁剪。设置 Radius 不会自动获得圆角子树裁剪。边框在矩形内部绘制，但**不占据布局空间**，需要内容留白时设置 Padding。Shadow 也不改变布局或点击区域。": "OverflowClip uses rectangular clipping. Radius does not imply rounded subtree clipping. Borders paint inside the rectangle but **consume no layout space**; use Padding for content spacing. Shadows do not affect layout or hit areas.",
    "`Hidden` 保留占位但不绘制/命中；Opacity 为 0 的子树也不会命中。透明背景颜色本身不会让几何点击区域消失。": "Hidden preserves layout but does not paint or hit-test. Opacity=0 subtrees are also excluded from hits. A transparent background alone does not remove the geometric hit area.",
    "只支持单行 flex；不支持 wrap、order、baseline、负间距、自动 margin、百分比间距、space-around/evenly、sticky、完整 CSS 布局。全部字段见[布局与样式值](/api/values)。": "Only single-line flex is supported. No wrapping, order, baseline, negative spacing, auto margins, percentage spacing, space-around/evenly, sticky positioning, or full CSS layout. See [layout and style values](/api/values) for all fields.",
    "下一步：[调整样式与主题](./style)。": "Next: [Styles and themes](./style).",
    "# 多窗口与独立更新": "# Multiple windows and independent updates",
    "这一节从主窗口打开子窗口，并分别修改它们的内容。每个子窗口都有自己的输入框和计数器。": "Open children from the main window and change each independently. Every child has its own input and counter.",
    "## 1. 打开两个子窗口": "## 1. Open two child windows",
    "运行后点击两次 **Create child**，然后在两个子窗口中输入不同文字、点击 **Increment this child**。各窗口的文字和数字互不影响。": "Click **Create child** twice, enter different text in each, and click **Increment this child**. Each window keeps independent text and numbers.",
    "创建窗口使用 `app.CreateWindow`：第一个参数设置标题和尺寸，第二个参数描述窗口中的控件。它在按钮回调中调用，不要放进构建函数，也不要从 goroutine 直接调用。": "Use app.CreateWindow: the first argument sets title and size, and the second describes the content. Call it inside a button callback, not a builder or background goroutine.",
    "## 2. 从主窗口更新子窗口": "## 2. Update a child from the main window",
    "点击主窗口的 **Update newest child**，最近打开且未关闭的子窗口数字加一。": "Click **Update newest child** in the main window to increment the most recently opened child that is still alive.",
    "| 要更新哪里 | 使用方式 |": "| Update target | How |",
    "| 当前窗口的组件回调 | 直接修改当前窗口的状态。 |": "| Current window component callback | Modify that window's state directly. |",
    "| 主窗口 | `app.Update` 包住状态修改。 |": "| Main window | Wrap state changes in app.Update. |",
    "| 某个子窗口 | 该 `window.Update` 包住状态修改。 |": "| A child window | Wrap state changes in that window.Update. |",
    "共享一个 Go 变量不会自动刷新所有窗口。Example在子窗口中使用 `app.Update` 更新主窗口的提示文字。": "Sharing a Go variable does not automatically refresh every window. The example uses app.Update from a child to update the main-window message.",
    "## 3. 关闭窗口": "## 3. Close windows",
    "取消子窗口的 **Allow close** 勾选，再点击关闭按钮，窗口会保留。重新勾选后即可关闭。": "Uncheck **Allow close** in a child, then attempt to close it: the window remains open. Check it again to allow closure.",
    "`Window.Close` 只关闭一个子窗口；`App.Close` 或关闭主窗口会结束整个应用，不逐一询问子窗口。提交更新时仍要处理错误，因为目标窗口可能已关闭。": "Window.Close affects one child. App.Close or closing the main window ends the whole app without asking each child. Handle update errors because the target may have closed.",
    "::: details 创建窗口时的两个注意点": "::: details Two details about window creation",
    "第一次构建发生在 `CreateWindow` 返回前。构建函数中不要立即读取外层尚未赋值的窗口句柄；Example只在之后的按钮回调中使用它。": "The first build occurs before CreateWindow returns. Do not immediately read an outer handle that has not been assigned yet; the example uses it only in later button callbacks.",
    "`OnShown` 同样在 `CreateWindow` 返回前执行，需要句柄时使用回调传入的 `*Window`。": "OnShown also runs before CreateWindow returns. Use the *Window argument passed to the callback.",
    "主题通过 `App.SetTheme` 统一切换；子窗口快捷键需要单独设置。更多配置、刷新方法、窗口尺寸与最大化操作见[窗口管理 API](/api/windows)。": "App.SetTheme changes themes across windows; child shortcuts need separate configuration. See [window management](/api/windows) for options, refresh methods, size, and maximization.",
    "下一步：[遇到问题时排查](./troubleshooting)。": "Next: [Troubleshooting](./troubleshooting).",
    "# 状态与事件": "# State and events",
    "这一节做一个计数器：每点一次按钮，数字加一。": "Build a counter that increments each time its button is clicked.",
    "## 1. 运行计数器": "## 1. Run the counter",
    "用下面的代码替换上一节的 `main.go`，执行 `go run .`。后面的完整Example也用这个方式运行。": "Replace the previous main.go with this program and run `go run .`. Use the same procedure for later complete examples.",
    "点击 **Add**，观察数字变化。": "Click **Add** and watch the number change.",
    "## 2. 看懂更新过程": "## 2. Understand the update",
    "1. `count` 保存当前数字，声明在 `app.Run` 外面。": "1. `count` stores the number outside `app.Run`.",
    "2. `OnPress` 在点击时执行 `count++`。": "2. `OnPress` executes `count++` on activation.",
    "3. DXUI 再次运行构建函数，`Label` 读到新数字，界面随之更新。": "3. DXUI runs the builder again; Label reads the new number and updates the UI.",
    "**状态放在构建函数外，修改放在事件回调里。** 如果把 `count := 0` 放进构建函数，每次更新都会归零。构建函数只负责描述界面，不要在里面启动任务或修改状态。": "**Keep state outside builders and change it in callbacks.** Declaring count := 0 inside the builder resets it on every rebuild. Builders only describe the UI; do not start tasks or change state there.",
    "试着把 `count++` 改成 `count += 2`，再运行一次。": "Try replacing count++ with count += 2 and run again.",
    "## 3. 保存输入的文字": "## 3. Retain text input",
    "输入框也使用同样的方式：`Value` 读取变量，`OnChange` 把新文字写回变量。": "Inputs follow the same pattern: Value reads the variable, and OnChange writes the new text back.",
    "// 放在 app.Run 外。": "// Declare outside app.Run.",
    "// 在构建函数返回的 Box 中添加这个控件。": "// Add this control to the Box returned by the builder.",
    "`next` 是输入框的完整新内容，不需要再拼接。上面的回调也可以简写为 `OnChange: dxui.Assign(&name)`。不写回新值，输入框就不会保留这次修改。": "next is the complete new input value, so do not concatenate it. The callback can be shortened to `OnChange: dxui.Assign(&name)`. Without writing the new value back, the input cannot retain the edit.",
    "普通组件回调修改本窗口状态后，无需手动刷新。后台任务使用 [App.Update](./async)；更新其它窗口见[多窗口](./multi-window)。": "Regular callbacks that change the current window need no manual refresh. Background work uses [App.Update](./async); for other windows see [multiple windows](./multi-window).",
    "::: details 列表中的控件为什么需要 Key": "::: details Why list controls need Key",
    "动态添加、删除或排序控件时，用稳定的业务 ID 设置 `Key`，让输入选区和焦点跟随正确的控件。不要用会变化的位置作为 ID。": "When inserting, deleting, or sorting controls, set Key to a stable business ID so selection and focus follow the right control. Do not use changing positions as IDs.",
    "Key 只在同一父级内有效；卸载后不保证保留运行时状态。业务值仍需放在控件外。完整规则见[组件共同契约](/api/common)。": "Keys are local to one parent; runtime state need not survive unmounting. Keep business data outside the control. See [common contracts](/api/common).",
    "输入长度限制、只读和禁用的区别见 [Input](/components/input)。": "See [Input](/components/input) for length limits, read-only behavior, and disabled state.",
    "下一步：[排列界面](./layout)。": "Next: [Arrange the UI](./layout).",
    "# 样式与主题": "# Styles and themes",
    "这一节调整控件外观，并用按钮切换明暗主题。": "Customize control appearance and switch between light and dark themes with a button.",
    "点击 **Switch theme** 切换主题。把鼠标移到按钮上，按钮会稍微变淡；按 Tab 可以看到键盘焦点效果。": "Click **Switch theme** to change themes. Hover over the button to make it slightly translucent; press Tab to see keyboard focus.",
    "## 2. 分清三个入口": "## 2. Choose the right setting",
    "| 想改什么 | 在哪里设置 |": "| What to change | Where |",
    "| 控件的尺寸、间距、背景 | 控件的 `Style`。 |": "| Control size, spacing, and background | The control's Style. |",
    "| 鼠标悬停时的外观 | `States.Hover`，Example中设置了透明度。 |": "| Hover appearance | States.Hover; the example changes opacity. |",
    "| 整个应用的配色 | 调用 `app.SetTheme`。 |": "| Application colors | Call app.SetTheme. |",
    "普通外观从 `Style` 开始设置。例如，下面是一个样式片段：": "Start with Style for normal appearance. For example:",
    "## 3. 切换主题": "## 3. Switch themes",
    "Example从 `LightTheme()` 或 `DarkTheme()` 取得完整主题，修改强调色，再传给 `app.SetTheme(next)`。主题中的颜色名称称为 Token，控件通过它们使用统一配色。": "The example starts from LightTheme() or DarkTheme(), changes the accent color, then calls app.SetTheme(next). Named theme values are tokens that give controls consistent colors.",
    "试着把Example中的 `Blue600` 改成 `Red600`。修改主题后要再次调用 `SetTheme`，只修改 Go map 不会更新界面；有多个窗口时，主题会传播到所有存活窗口。": "Try changing Blue600 to Red600. Call SetTheme after modifying the theme; editing a Go map alone does not update the UI. Theme changes propagate to all live windows.",
    "::: details 进阶：覆盖顺序、清除默认样式与 Token": "::: details Advanced: precedence, explicit clearing, and tokens",
    "### 实际覆盖顺序": "### Style precedence",
    "→ 局部 Style → 局部 States.Default": "→ local Style → local States.Default",
    "同一个活动状态内先应用组件主题补丁，再应用局部补丁。Disabled 抑制 Hover/Focus/Pressed，但受控 Checked 层仍保留在其下。状态补丁只支持绘制属性，不能让按钮 hover 时改变尺寸。": "Within an active state, component theme patches apply before local patches. Disabled suppresses Hover/Focus/Pressed; a controlled Checked layer remains below it. State patches only change painting, not dimensions on hover.",
    "### 显式零值": "### Explicit zero values",
    "Go 的零值经常表示“未设置”，并非“强制清空”。": "A Go zero value often means unset, not explicitly cleared.",
    "| 目的 | 写法 |": "| Goal | Setting |",
    "| 清除默认内边距 | `Padding: dxui.Padding(0)` |": "| Clear default padding | `Padding: dxui.Padding(0)` |",
    "| 强制透明度为 0 | `Opacity: dxui.Some(float32(0))` |": "| Explicit zero opacity | `Opacity: dxui.Some(float32(0))` |",
    "| 禁止缩小 | `Shrink: dxui.NoShrink()` |": "| Prevent shrinking | `Shrink: dxui.NoShrink()` |",
    "| 清除普通边框 | `Border: dxui.NoBorder()` |": "| Clear a normal border | `Border: dxui.NoBorder()` |",
    "| 清除主题阴影 | `Shadow: []dxui.Shadow{}`，nil 不清除 |": "| Clear theme shadows | `Shadow: []dxui.Shadow{}`; nil does not clear |",
    "| 连状态阴影一起清除 | `Force: dxui.StylePatch{Shadow: dxui.Some([]dxui.Shadow{})}` |": "| Also clear state shadows | `Force: dxui.StylePatch{Shadow: dxui.Some([]dxui.Shadow{})}` |",
    "清除焦点环会使键盘定位不明显；大多数场景保留默认 Focus 样式，只改普通外观即可。": "Removing focus rings makes keyboard position harder to see. Usually retain default Focus styling and change only normal appearance.",
    "### 字面值、Token 与颜色空间": "### Literals, tokens, and colors",
    "`RGBA` 返回原始 RGBAColor，适合 AppOptions.Background 和 Primitive 颜色表。`ColorRGBA` 返回显式 ColorValue，适合 Style.Background。已有 RGBAColor 用 LiteralColor 包装；TokenColor 指向主题颜色。Metric 是显式逻辑数值，TokenMetric 从主题读取指标。": "RGBA returns raw RGBAColor for AppOptions.Background and primitive color maps. ColorRGBA returns explicit ColorValue for Style.Background. Wrap existing RGBAColor with LiteralColor; TokenColor references the theme. Metric supplies an explicit logical number; TokenMetric reads a theme metric.",
    "`Color.Primitive.Blue600` 是调色板 token，`Color.Semantic.Accent` 表示强调用途。主题允许把语义色重新映射到另一原始色。旧的平铺常量也在[Token 参考](/api/tokens)中，旧 White/Black/Blue/Gray 与新调色板条目并非同一组值。": "Color.Primitive.Blue600 is a palette token; Color.Semantic.Accent is a semantic role. Themes may map semantic colors to other palette colors. Legacy flat constants are in the [token reference](/api/tokens); old White/Black/Blue/Gray are not identical to the new palette entries.",
    "主题先从 `LightTheme()` 或 `DarkTheme()` 取完整副本，再改映射；不要创建一个只含 Accent 的 Theme 期待自动补全其余项。SetTheme 校验缺失引用、循环和非法值，失败时整份拒绝。调用成功会复制 map/slice；之后修改应用原始 map 不会隐式更新界面，需要再次 SetTheme。相同或未使用 Token 的变化不必产生新帧。": "Start from a complete LightTheme() or DarkTheme() before editing maps. A Theme containing only Accent does not auto-fill missing entries. SetTheme rejects missing references, cycles, and invalid values. Accepted maps/slices are copied; editing the original map later does not update the UI without another SetTheme call. Equal themes or unused-token changes need not produce a frame.",
    "下一步：[把控件组合成表单](./forms)。完整配置见[主题 API](/api/theme)。": "Next: [Build a form](./forms). See [theme APIs](/api/theme) for full configuration.",
    "# 资源、性能与排错": "# Resources, performance, and troubleshooting",
    "遇到问题时，先查看终端中的错误，再按下面的现象定位。": "Check terminal errors first, then find the matching symptom below.",
    "## 窗口打不开": "## The window does not open",
    "确认在有桌面环境的系统中运行，并检查 `app.Run` 返回的错误。若错误与渲染器创建有关，可在 `AppOptions` 中加入下面的配置，再试一次：": "Check that a desktop session is available and inspect the error returned by app.Run. If renderer creation failed, add this configuration to AppOptions and retry:",
    "软件渲染仍需要窗口系统，不能在没有显示环境的服务器上直接打开窗口。": "Software rendering still needs a window system; it cannot open a window on a server without a display environment.",
    "## 界面不符合预期": "## The UI behaves unexpectedly",
    "| 现象 | 先这样检查 |": "| Symptom | Check first |",
    "| 输入的文字留不住 | `OnChange` 是否把新值写回变量，变量是否放在 `Run` 外。 |": "| Typed text does not stay | OnChange writes the value back and the variable is outside Run. |",
    "| 后台修改后界面不变 | 用 `App.Update` 或目标 `Window.Update` 提交赋值。 |": "| Background changes do not appear | Submit assignments through App.Update or the target Window.Update. |",
    "| 另一个窗口没有更新 | 对那个窗口单独调用 `Update` 或 `Invalidate`。 |": "| Another window stays stale | Call Update or Invalidate for that window. |",
    "| 控件撑不满窗口 | 用 `RunResponsive` 给根容器设置窗口宽高。 |": "| Controls do not fill the window | Use RunResponsive to set root width/height. |",
    "| 内容超出后不能滚动 | 使用 `Scroll`，并设置明确的视口高度。 |": "| Overflow does not scroll | Use Scroll with a definite viewport height. |",
    "| 列表排序后焦点跟错行 | 给控件设置稳定的业务 `Key`。 |": "| Focus follows the wrong reordered item | Use stable business Keys. |",
    "| 中文显示为方块 | 检查字体是否包含中文字形，见[字体配置](/api/resources-guide)。 |": "| CJK characters appear as boxes | Check font glyph coverage; see [fonts](/api/resources-guide). |",
    "| 悬停后颜色变了 | 检查 `States`，交互状态会覆盖普通 `Style`。 |": "| Hover changes a color | States override normal Style. |",
    "| 子窗口快捷键不起作用 | 设置该窗口的 `WindowOptions.Shortcuts`。 |": "| Child shortcuts do not work | Configure that child's WindowOptions.Shortcuts. |",
    "## 运行变慢或内存增加": "## Slow rendering or increasing memory",
    "先检查三件事：": "Start with these three checks:",
    "1. **事件回调是否执行耗时工作**：移到[后台任务](./async)。": "1. **Expensive work in callbacks**: Move it to a [background task](./async).",
    "2. **是否反复创建图片资源**：把 ImageSource 放到构建函数外复用，见 [Image](/components/image)。": "2. **Repeated image creation**: Reuse ImageSource outside builders; see [Image](/components/image).",
    "3. **是否一次显示大量列表项**：固定行高列表使用 [VirtualList](/components/virtual-list)。": "3. **Too many mounted list items**: Use [VirtualList](/components/virtual-list) for fixed-height rows.",
    "DXUI 在需要时更新界面，不需要定时调用 Update 来保持刷新。": "DXUI updates on demand; periodic Update calls are not needed to keep it refreshing.",
    "::: details 进一步查看性能数据和能力限制": "::: details Performance metrics and capability limits",
    "事件循环按事件和截止时间唤醒，不固定帧率空转。不要写定时 Update 来“保持刷新”。背景任务在结果或真实进度改变时提交；ProgressBar 本身不启动动画。每个窗口独立使用资源缓存预算，多个窗口的总容量会增加。资源缓存预算不等于当前工作集的硬上限，正在显示的图片仍占资源。": "The event loop wakes for events and deadlines, not fixed-rate idle frames. Submit updates for real result/progress changes, not to keep refreshing. ProgressBar has no internal animation timer. Each window has independent resource budgets, so total capacity increases with more windows. Cache budgets are not hard working-set caps; visible images still consume resources.",
    "使用 `AppOptions.Diagnostics: true` 取得可比较的快照，观察 Build/Layout/Paint 与 FrameCount 的区别；空闲无变化时不应要求持续增加帧数。`Diagnostics()` 可跨 goroutine 读取，但不要把统计轮询作为生产 UI 更新机制。[诊断字段说明](/api/application#cachebudgets-与-runtimediagnostics)。": "Enable `AppOptions.Diagnostics: true` for comparable snapshots. Distinguish Build/Layout/Paint from FrameCount; frame counts need not grow while idle. Diagnostics() is concurrent-safe, but do not use polling to drive production UI refresh. See [diagnostic fields](/api/application#cachebudgets-and-runtimediagnostics).",
    "VirtualList 只构建可见区及 overscan；新 Count/Version 快照仍需 O(N) 验证所有键，不代表所有操作都是 O(可见行数)。它不支持可变行高，离屏卸载会丢失运行时编辑/焦点状态。": "VirtualList builds only visible and overscan rows, but a new Count/Version snapshot still needs O(N) key validation. It does not support variable row heights; unmounting offscreen rows discards runtime editor/focus state.",
    "### 当前能力边界": "### Current capability limits",
    "没有完整 CSS Flexbox、富文本、grapheme-cluster 编辑、bidi/RTL/复杂脚本 shaping、平台无障碍桥接、动画图片、SVG 解析、RadioGroup、三态复选框、TabPanel、模态焦点陷阱。相关组合应使用现有公开类型实现，并在产品需要超出这些能力时评估框架扩展。": "No full CSS Flexbox, rich text, grapheme-cluster editing, bidi/RTL/complex-script shaping, platform accessibility bridge, animated images, SVG parsing, RadioGroup, tri-state checkbox, TabPanel, or modal focus trap. Compose existing public types where appropriate and assess framework extensions when requirements exceed them.",
    "本站的自动验证包括源码 API 对照、完整程序编译和 VitePress 构建。原生窗口的输入、真实 IME、DPI 切换、视觉效果与平台发布仍需手动测试；“可编译”不等于“已在所有平台运行”。": "Automated checks compare source APIs, compile complete programs, and build VitePress. Native input, real IME, DPI changes, visuals, and platform distribution still require manual testing. Compiling is not the same as running on every platform.",
    "接下来按需要查[组件Example](/components/)或[公开 API](/api/)。": "Continue with [component examples](/components/) or the [public API](/api/).",
    "  text: 声明式构建，按需渲染。": "  text: Declarative views. Render on demand.",
    "  tagline: 面向跨平台桌面应用的 Go GUI 框架，同时支持 Windows、MacOS、Linux。融合声明式视图与保留式更新，以事件驱动运行；支持无 CGO 构建、多窗口管理与类型安全的组件、布局和主题。": "  tagline: A Go GUI framework for Windows, macOS, and Linux. Declarative views meet retained updates in an event-driven runtime, with CGO-free builds, multiple windows, and typed components, layouts, and themes.",
    "    alt: DXUI 标志": "    alt: DXUI logo",
    "      text: 运行第一个窗口": "      text: Run your first window",
    "      text: 查看组件Example": "      text: Explore components",
    "  - title: 声明式接口，保留式内核": "  - title: Declarative API, retained core",
    "    details: 以不可变 View 描述界面，由框架协调新旧组件树。稳定的 Key 保留焦点、编辑与交互状态，业务数据始终由应用掌握。": "    details: Describe the UI with immutable Views and let the framework reconcile the tree. Stable keys retain focus and editing state while your application owns its data.",
    "    linkText: 理解视图与状态": "    linkText: Understand views and state",
    "  - title: 事件驱动的按需更新": "  - title: Event-driven updates",
    "    details: 多个窗口共用事件循环，分别维护视图与交互状态，按需更新各自界面。空闲时不持续重绘。区分布局与绘制变化，颜色等视觉更新无需重新测量布局。": "    details: Windows share an event loop while keeping independent UI and interaction state. Idle windows do not repaint continuously, and paint-only changes avoid unnecessary layout measurement.",
    "    linkText: 了解运行与性能行为": "    linkText: Explore runtime behavior",
    "  - title: 无需 CGO 的构建方式": "  - title: Build without CGO",
    "    details: 使用 CGO_ENABLED=0 构建框架与应用，沿用 Go 的模块管理和编译流程，为不同桌面平台构建应用产物。": "    details: Build the framework and applications with CGO_ENABLED=0, using familiar Go modules and compilation workflows for each desktop platform.",
    "    linkText: 查看构建与平台要求": "    linkText: Run and build",
    "  - title: 类型安全的布局与主题": "  - title: Typed layouts and themes",
    "    details: 用具体 Go 类型配置布局、样式和交互状态。Primitive、Semantic、Component 三层 Token 支持统一定制，主题校验后原子替换。": "    details: Configure layout, appearance, and interaction states with concrete Go types. Primitive, Semantic, and Component tokens provide consistent customization with validated theme updates.",
    "    linkText: 探索样式与主题": "    linkText: Explore styles and themes",
    "  - title: 面向长列表的虚拟化": "  - title: Virtualized long lists",
    "    details: 固定行高 VirtualList 只挂载可见区域与预留行，支持稳定业务键和滚动锚定，让大数据列表的视图规模保持可控。": "    details: Fixed-height VirtualList mounts only visible and overscan rows. Stable keys and scroll anchoring keep large datasets manageable.",
    "    linkText: 使用虚拟列表": "    linkText: Use virtual lists",
    "  - title: 按需链接的矢量图标": "  - title: Icons linked on demand",
    "    details: 提供 2,066 个官方图标名称与别名，支持主题着色和描边宽度配置。直接引用所需构造器，未使用的图标可由 Go 链接器移除。": "    details: Choose from 2,066 official icon names and aliases with theme colors and configurable stroke widths. Reference only the constructors you need so the Go linker can discard unused icons.",
    "    linkText: 使用图标资源": "    linkText: Use icon resources",
    "<p class=\"home-eyebrow\">从这里开始</p>": "<p class=\"home-eyebrow\">Start here</p>",
    "<h2 id=\"first-window\">一个 main.go，<br>一个桌面窗口。</h2>": "<h2 id=\"first-window\">One main.go.<br>One desktop window.</h2>",
    "<p>组件组合、Application lifecycle与事件回调，统一在类型明确的 Go API 中。下面的完整程序展示了一个窗口从构建到关闭的基本流程。</p>": "<p>Compose components and handle lifecycle events through a typed Go API. This complete program takes a window from creation to shutdown.</p>",
    "<li><strong>准备环境</strong><span>安装 Go 1.25+，创建模块并添加 dxui 依赖。</span></li>": "<li><strong>Set up</strong><span>Install Go 1.25+, create a module, and add DXUI.</span></li>",
    "<li><strong>描述界面</strong><span>在 Run 的构建函数里组合文字、容器和按钮。</span></li>": "<li><strong>Describe the UI</strong><span>Compose text, containers, and buttons in the Run builder.</span></li>",
    "<li><strong>运行与交互</strong><span>执行 go run .，点击 Close 结束应用。</span></li>": "<li><strong>Run and interact</strong><span>Run go run . and click Close to exit.</span></li>",
    "<a class=\"home-text-link\" href=\"/guide/getting-started\">查看安装步骤与运行说明 <span aria-hidden=\"true\">→</span></a>": "<a class=\"home-text-link\" href=\"/guide/getting-started\">View setup and running instructions <span aria-hidden=\"true\">→</span></a>",
    "<div class=\"home-code-title\"><span>main.go</span><span>完整Example</span></div>": "<div class=\"home-code-title\"><span>main.go</span><span>Complete example</span></div>",
    "<p class=\"home-eyebrow\">按你的进度阅读</p>": "<p class=\"home-eyebrow\">Learn at your pace</p>",
    "<h2 id=\"explore-docs\">从学会使用，到随时查阅。</h2>": "<h2 id=\"explore-docs\">From getting started to looking it up.</h2>",
    "<span class=\"home-path-number\">01 / 学习</span>": "<span class=\"home-path-number\">01 / Learn</span>",
    "<h3>跟着教程构建应用</h3>": "<h3>Build along with the tutorials</h3>",
    "<p>窗口 → 状态与事件 → 布局与样式 → 表单 → 后台任务。每一步都解释代码为什么这样写。</p>": "<p>Window → state and events → layout and styles → forms → background tasks. Each step explains how the code works.</p>",
    "<span class=\"home-path-action\">开始指南 <span aria-hidden=\"true\">↗</span></span>": "<span class=\"home-path-action\">Start the guide <span aria-hidden=\"true\">↗</span></span>",
    "<span class=\"home-path-number\">02 / 实践</span>": "<span class=\"home-path-number\">02 / Practice</span>",
    "<h3>找到需要的组件</h3>": "<h3>Find the right component</h3>",
    "<p>23 种组件，附可独立运行的程序、完整属性与交互规则。先复制运行，再按需求调整。</p>": "<p>23 components with standalone programs, complete props, and interaction rules. Run an example, then adapt it.</p>",
    "<span class=\"home-path-action\">浏览完整组件Example <span aria-hidden=\"true\">↗</span></span>": "<span class=\"home-path-action\">Browse component examples <span aria-hidden=\"true\">↗</span></span>",
    "<span class=\"home-path-number\">03 / 查阅</span>": "<span class=\"home-path-number\">03 / Reference</span>",
    "<h3>查清 API 的实际行为</h3>": "<h3>Understand each API</h3>",
    "<p>查找用途、参数、默认值与限制。从Application lifecycle到主题、字体、图片和全部官方图标。</p>": "<p>Look up parameters, defaults, and limitations for lifecycle, themes, fonts, images, and every official icon.</p>",
    "<span class=\"home-path-action\">打开公开 API 索引 <span aria-hidden=\"true\">↗</span></span>": "<span class=\"home-path-action\">Open the API index <span aria-hidden=\"true\">↗</span></span>",
    "<div><p class=\"home-eyebrow\">常用入口</p><h2 id=\"common-tasks\">你想实现什么？</h2></div>": "<div><p class=\"home-eyebrow\">Common tasks</p><h2 id=\"common-tasks\">What are you building?</h2></div>",
    "<a href=\"/guide/forms\">构建输入表单 <span aria-hidden=\"true\">→</span></a>": "<a href=\"/guide/forms\">Build an input form <span aria-hidden=\"true\">→</span></a>",
    "<a href=\"/components/virtual-list\">展示大量列表数据 <span aria-hidden=\"true\">→</span></a>": "<a href=\"/components/virtual-list\">Display large datasets <span aria-hidden=\"true\">→</span></a>",
    "<a href=\"/guide/style\">定制样式与明暗主题 <span aria-hidden=\"true\">→</span></a>": "<a href=\"/guide/style\">Customize styles and themes <span aria-hidden=\"true\">→</span></a>",
    "<a href=\"/api/icons\">查找官方图标 <span aria-hidden=\"true\">→</span></a>": "<a href=\"/api/icons\">Find official icons <span aria-hidden=\"true\">→</span></a>",
    "<a href=\"/guide/async\">接入后台任务 <span aria-hidden=\"true\">→</span></a>": "<a href=\"/guide/async\">Run background tasks <span aria-hidden=\"true\">→</span></a>",
    "<a href=\"/guide/multi-window\">管理多个窗口 <span aria-hidden=\"true\">→</span></a>": "<a href=\"/guide/multi-window\">Manage multiple windows <span aria-hidden=\"true\">→</span></a>",
    "<a href=\"/guide/troubleshooting\">排查运行与交互问题 <span aria-hidden=\"true\">→</span></a>": "<a href=\"/guide/troubleshooting\">Troubleshoot runtime behavior <span aria-hidden=\"true\">→</span></a>"
  }
}
